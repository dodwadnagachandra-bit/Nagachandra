---
phase: 19-hmi-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hmi_server/frontend/package.json
  - src/hmi_server/frontend/vite.config.ts
  - src/hmi_server/frontend/index.html
  - src/hmi_server/frontend/src/main.tsx
  - src/hmi_server/frontend/src/App.tsx
  - src/hmi_server/frontend/src/app.css
  - src/hmi_server/frontend/src/types/telemetry.ts
  - src/hmi_server/frontend/src/types/api.ts
  - src/hmi_server/frontend/src/types/enums.ts
  - src/hmi_server/frontend/src/components/Layout.tsx
  - src/hmi_server/frontend/src/components/Sidebar.tsx
  - src/hmi_server/frontend/src/components/ConnectionIndicator.tsx
  - src/hmi_server/frontend/src/screens/Dashboard.tsx
  - src/hmi_server/frontend/src/screens/BmsScreen.tsx
  - src/hmi_server/frontend/src/screens/PcsScreen.tsx
  - src/hmi_server/frontend/src/screens/AlarmScreen.tsx
  - src/hmi_server/frontend/src/screens/ControlScreen.tsx
  - src/hmi_server/frontend/src/screens/EnergyScreen.tsx
  - src/hmi_server/frontend/src/screens/SettingsScreen.tsx
  - src/hmi_server/frontend/vitest.config.ts
  - src/hmi_server/frontend/src/__tests__/Layout.test.tsx
autonomous: true
requirements: [HMI-12, HMI-13]

must_haves:
  truths:
    - "App renders a persistent sidebar with 7 navigation tabs (Settings hidden by default)"
    - "Sidebar collapses to icons-only at 10in breakpoint, shows labels at 15in"
    - "Clicking a tab navigates to the correct screen without page reload"
    - "Connection status indicator (green/red/yellow dot) is visible in sidebar"
    - "Dark theme applied globally -- bg-slate-900 body, slate color palette"
    - "All tap targets are at least 44x44px"
  artifacts:
    - path: "src/hmi_server/frontend/src/types/telemetry.ts"
      provides: "TypeScript interfaces for all 6 WebSocket telemetry topics"
      exports: ["SystemTelemetry", "BmsRackTelemetry", "PcsTelemetry", "GpioTelemetry", "MeterTelemetry", "BtmsTelemetry", "WsMessage"]
    - path: "src/hmi_server/frontend/src/types/api.ts"
      provides: "TypeScript interfaces for all REST API requests and responses"
      exports: ["LoginRequest", "LoginResponse", "ZmqResponse", "ModeChangeRequest", "ManualSetpointRequest", "AcknowledgeRequest", "ActiveAlarm", "ScheduleConfig", "TimeWindow"]
    - path: "src/hmi_server/frontend/src/types/enums.ts"
      provides: "Display name mappings for control state and PCS state"
      exports: ["CONTROL_STATE_NAMES", "PCS_STATE_NAMES"]
    - path: "src/hmi_server/frontend/src/components/Layout.tsx"
      provides: "Shell layout with sidebar + content area"
    - path: "src/hmi_server/frontend/src/components/Sidebar.tsx"
      provides: "Tab navigation with lucide-react icons"
    - path: "src/hmi_server/frontend/src/App.tsx"
      provides: "React Router with lazy-loaded screen routes"
  key_links:
    - from: "src/hmi_server/frontend/src/App.tsx"
      to: "src/hmi_server/frontend/src/components/Layout.tsx"
      via: "Route layout wrapper"
      pattern: "<Layout>"
    - from: "src/hmi_server/frontend/src/components/Sidebar.tsx"
      to: "react-router-dom"
      via: "NavLink for active tab highlighting"
      pattern: "NavLink"
---

<objective>
Foundation: dependencies, TypeScript types, router, layout shell, sidebar navigation, dark theme, connection indicator, test infrastructure.

Purpose: Establish the project structure, all shared TypeScript interfaces, routing, and the visual shell that all subsequent plans build inside. Every later plan imports types from here and renders inside the Layout component.

Output: Working app with sidebar navigation between 7 placeholder screens, dark theme, responsive 10"/15" breakpoints, and Vitest test infrastructure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-hmi-frontend/19-CONTEXT.md
@.planning/phases/19-hmi-frontend/19-RESEARCH.md
@src/hmi_server/frontend/package.json
@src/hmi_server/frontend/vite.config.ts
@src/hmi_server/frontend/src/main.tsx
@src/hmi_server/frontend/src/App.tsx
@src/hmi_server/frontend/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, create types, dark theme CSS, Vite proxy, test infrastructure</name>
  <files>
    src/hmi_server/frontend/package.json
    src/hmi_server/frontend/vite.config.ts
    src/hmi_server/frontend/src/app.css
    src/hmi_server/frontend/src/main.tsx
    src/hmi_server/frontend/src/types/telemetry.ts
    src/hmi_server/frontend/src/types/api.ts
    src/hmi_server/frontend/src/types/enums.ts
    src/hmi_server/frontend/vitest.config.ts
  </files>
  <action>
    1. Install runtime dependencies:
       ```bash
       cd src/hmi_server/frontend && bun add react-router-dom chart.js react-chartjs-2 lucide-react
       ```

    2. Install dev/test dependencies:
       ```bash
       cd src/hmi_server/frontend && bun add -D vitest @testing-library/react @testing-library/jest-dom jsdom @testing-library/user-event
       ```

    3. Update `package.json` scripts: set `"test": "vitest"` (replaces the echo placeholder).

    4. Create `vitest.config.ts`:
       ```typescript
       import { defineConfig } from "vitest/config";
       import react from "@vitejs/plugin-react";
       export default defineConfig({
         plugins: [react()],
         test: { environment: "jsdom", globals: true, setupFiles: [] },
       });
       ```

    5. Create `src/app.css` with Tailwind v4 CSS-first config:
       ```css
       @import "tailwindcss";

       @theme {
         --color-bg-primary: #0f172a;
         --color-bg-secondary: #1e293b;
         --color-bg-card: #334155;
         --color-text-primary: #f1f5f9;
         --color-text-secondary: #94a3b8;
         --color-accent: #3b82f6;
         --color-success: #22c55e;
         --color-warning: #eab308;
         --color-danger: #ef4444;
         --color-orange: #f97316;
       }

       html { background-color: var(--color-bg-primary); color: var(--color-text-primary); }
       ```
       Import `./app.css` in `main.tsx` (add `import "./app.css";` before App import).

    6. Update `vite.config.ts` to add dev proxy for backend:
       ```typescript
       server: {
         proxy: {
           "/ws/telemetry": { target: "ws://localhost:8081", ws: true },
           "/api": { target: "http://localhost:8080" },
         },
       },
       ```

    7. Create `src/types/telemetry.ts` with ALL WebSocket telemetry interfaces exactly as specified in 19-RESEARCH.md:
       - `WsMessage` (envelope: topic, data, ts)
       - `SystemTelemetry` (control_state, total_soc, total_power_kw, etc.)
       - `BmsRackTelemetry` (pack_v, pack_i, pack_soc, min_cell_v, max_cell_v, etc.)
       - `PcsTelemetry` (ac_voltage, ac_current, active_power, dc_voltage, state, fault_code, etc.)
       - `GpioTelemetry` (di[], do_state[])
       - `MeterTelemetry` (voltage, current, active_power, frequency, energy_import, energy_export, etc.)
       - `BtmsTelemetry` (inlet_temp, outlet_temp, fan_speed_pct, cooling_active)
       - `TelemetryState` (system, pcs, gpio, meter, btms as nullable, bmsRacks as Record<string, BmsRackTelemetry>, lastUpdate)
       - `TelemetryAction` union type for reducer
       - `ConnectionStatus` type: "connected" | "disconnected" | "reconnecting"
       All exported.

    8. Create `src/types/api.ts` with ALL REST API interfaces from 19-RESEARCH.md:
       - `LoginRequest`, `LoginResponse`
       - `ZmqResponse`
       - `ModeChangeRequest`, `ManualSetpointRequest`, `SourcePriorityRequest`, `MaintenanceRequest`
       - `AcknowledgeRequest`, `ActiveAlarm`
       - `ScheduleConfig`, `TimeWindow`
       All exported.

    9. Create `src/types/enums.ts` with display name maps:
       - `CONTROL_STATE_NAMES: Record<number, string>` (1=IDLE through 7=MAINTENANCE)
       - `PCS_STATE_NAMES: Record<number, string>` (0=OFF through 3=FAULT)
       All exported.
  </action>
  <verify>
    <automated>cd src/hmi_server/frontend && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>All deps installed. Type files compile. Tailwind dark theme renders. Vite proxy configured. Vitest runnable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layout shell, sidebar navigation, router, connection indicator, placeholder screens</name>
  <files>
    src/hmi_server/frontend/src/App.tsx
    src/hmi_server/frontend/src/components/Layout.tsx
    src/hmi_server/frontend/src/components/Sidebar.tsx
    src/hmi_server/frontend/src/components/ConnectionIndicator.tsx
    src/hmi_server/frontend/src/screens/Dashboard.tsx
    src/hmi_server/frontend/src/screens/BmsScreen.tsx
    src/hmi_server/frontend/src/screens/PcsScreen.tsx
    src/hmi_server/frontend/src/screens/AlarmScreen.tsx
    src/hmi_server/frontend/src/screens/ControlScreen.tsx
    src/hmi_server/frontend/src/screens/EnergyScreen.tsx
    src/hmi_server/frontend/src/screens/SettingsScreen.tsx
    src/hmi_server/frontend/src/__tests__/Layout.test.tsx
    src/hmi_server/frontend/index.html
  </files>
  <behavior>
    - Test 1: Layout renders sidebar and content area
    - Test 2: Sidebar renders 6 visible navigation tabs (Settings hidden by default)
    - Test 3: Clicking a tab navigates to correct route (Dashboard at "/", BMS at "/bms", etc.)
    - Test 4: Active tab is visually highlighted
    - Test 5: ConnectionIndicator renders with correct color class based on status prop (green/red/yellow)
    - Test 6: Sidebar renders icons from lucide-react
  </behavior>
  <action>
    1. Create 7 placeholder screen components, each as a simple default export:
       - `Dashboard.tsx`: `<div data-testid="screen-dashboard"><h2>Dashboard</h2></div>`
       - `BmsScreen.tsx`: `<div data-testid="screen-bms"><h2>Battery</h2></div>`
       - `PcsScreen.tsx`: `<div data-testid="screen-pcs"><h2>Inverter</h2></div>`
       - `AlarmScreen.tsx`: `<div data-testid="screen-alarms"><h2>Alarms</h2></div>`
       - `ControlScreen.tsx`: `<div data-testid="screen-control"><h2>Control</h2></div>`
       - `EnergyScreen.tsx`: `<div data-testid="screen-energy"><h2>Energy</h2></div>`
       - `SettingsScreen.tsx`: `<div data-testid="screen-settings"><h2>Settings</h2></div>`
       These will be replaced with real implementations in Plans 03-05.

    2. Create `ConnectionIndicator.tsx`:
       - Props: `{ status: ConnectionStatus }` (from types/telemetry.ts)
       - Renders a 12px circle with Tailwind classes: `bg-success` (connected), `bg-danger` (disconnected), `bg-warning` (reconnecting)
       - Includes screen-reader-only text label
       - Minimum 44x44px tap target area (padded container)

    3. Create `Sidebar.tsx`:
       - Uses `NavLink` from react-router-dom for each tab
       - 7 tabs with lucide-react icons: LayoutGrid (Dashboard), Battery (BMS), Zap (PCS), Bell (Alarms), SlidersHorizontal (Control), BarChart3 (Energy), Settings (Settings)
       - Each NavLink has `min-h-[44px] min-w-[44px]` for touch targets
       - Active tab gets accent background (`bg-accent` or `bg-blue-600`)
       - No hover states. Use `active:` pseudo for pressed state
       - Responsive: on widths < 1280px show icons only (w-[60px]), on >= 1280px show icons + text labels (w-[200px]). Use Tailwind responsive prefix (custom breakpoint or lg:).
       - Settings tab: accept `showSettings` boolean prop -- if false, do not render Settings tab
       - Alarm badge: accept `unackedCount` number prop -- if > 0, show red dot on Bell icon with count
       - ConnectionIndicator rendered at bottom of sidebar
       - Props: `{ showSettings: boolean; unackedCount: number; connectionStatus: ConnectionStatus }`

    4. Create `Layout.tsx`:
       - Renders Sidebar on the left, `<Outlet />` on the right (filling remaining viewport)
       - Full viewport height (`h-screen`), flex row layout
       - Content area has `bg-bg-primary` and `overflow-y-auto`
       - Pass through showSettings, unackedCount, connectionStatus props (hardcode defaults for now: showSettings=false, unackedCount=0, connectionStatus="disconnected")

    5. Rewrite `App.tsx`:
       - Import `BrowserRouter`, `Routes`, `Route` from react-router-dom
       - Use `React.lazy` + `Suspense` for each screen import
       - Layout wraps all routes as a parent route element
       - Routes: "/" -> Dashboard, "/bms" -> BmsScreen, "/pcs" -> PcsScreen, "/alarms" -> AlarmScreen, "/control" -> ControlScreen, "/energy" -> EnergyScreen, "/settings" -> SettingsScreen
       - Suspense fallback: simple loading div with dark theme styling

    6. Update `index.html`: add `class="dark"` to `<html>` tag (Tailwind dark mode selector).

    7. Write `__tests__/Layout.test.tsx` testing the behaviors listed above. Use `MemoryRouter` from react-router-dom for testing navigation. Use `@testing-library/react` render + screen queries.
  </action>
  <verify>
    <automated>cd src/hmi_server/frontend && bun run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Sidebar renders 6 tabs (Settings hidden when showSettings=false)
    - Tab navigation works between all 7 routes
    - ConnectionIndicator shows correct colors
    - Dark theme applied to body and all components
    - 44x44px minimum tap targets on all interactive elements
    - Responsive sidebar: icons-only on small screens, icons+labels on large
    - All tests pass
    - `bun run build` succeeds
  </done>
</task>

</tasks>

<verification>
```bash
cd src/hmi_server/frontend && bun run build && bun run test -- --run
```
Build must succeed with zero errors. All Layout tests pass.
</verification>

<success_criteria>
- `bun run build` produces dist/ with working React app
- `bun run test -- --run` passes all Layout/Sidebar/ConnectionIndicator tests
- App renders sidebar with 6 navigation tabs and dark theme
- Navigating between tabs shows correct placeholder screens
- TypeScript types for all telemetry and API contracts are importable
</success_criteria>

<output>
After completion, create `.planning/phases/19-hmi-frontend/19-01-SUMMARY.md`
</output>
