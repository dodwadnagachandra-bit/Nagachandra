---
phase: 12-logging
plan: 06
type: execute
wave: 4
depends_on: ["12-02", "12-03", "12-04", "12-05"]
files_modified:
  - src/logger/python/src/ems_logger/__main__.py
  - deploy/systemd/logger.service
  - src/logger/python/tests/test_logger_integration.py
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06
  - LOG-07
  - LOG-08
  - LOG-09

must_haves:
  truths:
    - "Logger starts as a single asyncio process running 4 concurrent tasks"
    - "Telemetry writer, event consumer, query server, and cleanup all run concurrently"
    - "SIGTERM/SIGINT triggers graceful shutdown closing all ZMQ sockets and flushing files"
    - "Logger starts after data_manager and comm_manager services"
    - "Logger handles missing config gracefully with sensible defaults"
    - "End-to-end: ZMQ messages in -> Parquet/JSONL on disk -> DuckDB queries return data"
  artifacts:
    - path: "src/logger/python/src/ems_logger/__main__.py"
      provides: "Logger service entry point wiring all components"
      min_lines: 80
    - path: "deploy/systemd/logger.service"
      provides: "Updated systemd service file with correct dependencies"
    - path: "src/logger/python/tests/test_logger_integration.py"
      provides: "Integration tests: publish telemetry -> write Parquet -> query via DuckDB"
      min_lines: 80
  key_links:
    - from: "src/logger/python/src/ems_logger/__main__.py"
      to: "src/logger/python/src/ems_logger/telemetry_writer.py"
      via: "creates TelemetryWriter and runs as asyncio task"
      pattern: "TelemetryWriter"
    - from: "src/logger/python/src/ems_logger/__main__.py"
      to: "src/logger/python/src/ems_logger/event_writer.py"
      via: "creates EventConsumer and runs as asyncio task"
      pattern: "EventConsumer"
    - from: "src/logger/python/src/ems_logger/__main__.py"
      to: "src/logger/python/src/ems_logger/query_handler.py"
      via: "creates QueryServer and runs as asyncio task"
      pattern: "QueryServer"
    - from: "src/logger/python/src/ems_logger/__main__.py"
      to: "src/logger/python/src/ems_logger/cleanup.py"
      via: "creates RetentionManager and runs periodic cleanup"
      pattern: "RetentionManager"
---

<objective>
Logger main entry point, systemd service, and integration tests: wire all components together, handle lifecycle, and verify end-to-end data flow.

Purpose: This is the final assembly -- connecting telemetry writer, event consumer, query handler, and cleanup into a single service that starts via systemd and handles graceful shutdown. Integration tests verify the complete pipeline from ZMQ message to DuckDB query result.
Output: Working logger service entry point, updated systemd service, and integration tests proving the full pipeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-logging/12-CONTEXT.md
@.planning/phases/12-logging/12-RESEARCH.md
@.planning/phases/12-logging/12-01-SUMMARY.md
@.planning/phases/12-logging/12-02-SUMMARY.md
@.planning/phases/12-logging/12-03-SUMMARY.md
@.planning/phases/12-logging/12-04-SUMMARY.md
@.planning/phases/12-logging/12-05-SUMMARY.md

@src/logger/python/src/ems_logger/telemetry_writer.py
@src/logger/python/src/ems_logger/event_writer.py
@src/logger/python/src/ems_logger/query_handler.py
@src/logger/python/src/ems_logger/cleanup.py
@src/logger/python/src/ems_logger/config.py
@src/common/python/src/ems_common/ipc.py
@deploy/systemd/logger.service
@src/data_manager/python/src/ems_data_manager/__main__.py

<interfaces>
From telemetry_writer.py (Plan 02):
```python
class TelemetryWriter:
    def __init__(self, config: LoggerConfig, zmq_ctx, topology: dict, endpoint: str | None = None): ...
    async def run(self) -> None: ...
    def close(self) -> None: ...
```

From event_writer.py (Plan 03):
```python
class EventConsumer:
    def __init__(self, config: LoggerConfig, zmq_ctx, endpoint: str | None = None): ...
    async def run(self) -> None: ...
    def close(self) -> None: ...
```

From query_handler.py (Plan 04):
```python
class QueryServer:
    def __init__(self, config: LoggerConfig, zmq_ctx, endpoint: str | None = None): ...
    async def run(self) -> None: ...
    def close(self) -> None: ...
```

From cleanup.py (Plan 05):
```python
class RetentionManager:
    def __init__(self, config: LoggerConfig): ...
    def startup_recovery(self) -> None: ...
    async def run_periodic(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logger __main__.py entry point with asyncio lifecycle</name>
  <files>
    src/logger/python/src/ems_logger/__main__.py,
    deploy/systemd/logger.service
  </files>
  <action>
    1. Create __main__.py following existing patterns (data_manager __main__.py):
       - Parse CLI args: --config (path to logger_config.yaml, default config/logger_config.yaml)
       - Load LoggerConfig from YAML
       - Load topology from system_config.yaml (cluster_count, racks_per_cluster, modules_per_rack, cells_per_module) for Parquet schema generation. Gracefully handle missing system_config (log warning, use defaults).
       - Create zmq.asyncio.Context
       - Run RetentionManager.startup_recovery() synchronously (delete .tmp files before starting)
       - Create 4 async tasks: TelemetryWriter.run(), EventConsumer.run(), QueryServer.run(), RetentionManager.run_periodic()
       - Register SIGTERM and SIGINT handlers that cancel all tasks
       - On shutdown: close all components (TelemetryWriter, EventConsumer, QueryServer), flush files
       - Set up logging: logging.basicConfig with structured format, logger name "ems_logger"
       - asyncio.run(main())

    2. Update deploy/systemd/logger.service:
       - Remove "C++ component launched as subprocess" comment (no C component needed in Phase 12)
       - Add After=ems-data-manager.service After=comm_manager.service (needs data flowing)
       - Add Wants=ems-data-manager.service (soft dependency)
       - Keep Restart=on-failure, RestartSec=5
       - Add Environment=EMS_CONFIG_DIR=/opt/ems/config (or use --config CLI arg)
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && python -c "import ast; ast.parse(open('src/logger/python/src/ems_logger/__main__.py').read()); print('syntax OK')" && grep -q "After=.*data.manager" deploy/systemd/logger.service && echo "systemd OK"</automated>
  </verify>
  <done>Logger entry point creates and runs all 4 components concurrently; handles SIGTERM/SIGINT gracefully; systemd service has correct dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests -- end-to-end telemetry and query pipeline</name>
  <files>
    src/logger/python/tests/test_logger_integration.py
  </files>
  <behavior>
    - Publish telemetry via ZMQ PUB -> TelemetryWriter writes Parquet -> DuckDB time_series query returns the data
    - Push events via ZMQ PUSH -> EventConsumer writes JSONL -> event_log query returns the events
    - Crash recovery: create .tmp files, startup_recovery deletes them
    - Parquet files have Snappy compression and correct directory structure
  </behavior>
  <action>
    Create test_logger_integration.py with end-to-end tests using tcp://127.0.0.1 endpoints (avoid /run/ems dependency):

    1. test_telemetry_to_parquet_to_query:
       - Create TelemetryWriter with tcp:// SUB endpoint
       - Publish 5 seconds of sample telemetry via ZMQ PUB (using encode_telemetry from ipc.py)
       - Wait for writer to flush
       - Query via query_time_series, verify data matches published values
       - Verify files exist in correct directory structure
       - Verify Snappy compression in Parquet metadata

    2. test_events_to_jsonl_to_query:
       - Create EventConsumer with tcp:// PULL endpoint
       - Push 10 events via ZMQ PUSH (using encode_event from ipc.py)
       - Wait for consumer to flush
       - Query via query_event_log with severity and source filters
       - Verify events match pushed data

    3. test_crash_recovery_on_startup:
       - Create .tmp files in data directory
       - Run RetentionManager.startup_recovery()
       - Verify .tmp files deleted
       - Verify .parquet files untouched

    4. test_query_server_round_trip:
       - Start QueryServer with tcp:// REP endpoint
       - Write some Parquet data directly
       - Send ZMQ REQ with time_series query
       - Verify response contains correct data

    Use pytest-asyncio for async tests. Use tcp://127.0.0.1:{random_port} for ZMQ to avoid /run/ems dependency.
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv run pytest src/logger/python/tests/test_logger_integration.py -x -v</automated>
  </verify>
  <done>End-to-end pipeline works: ZMQ telemetry -> Parquet -> DuckDB query; ZMQ events -> JSONL -> event query; crash recovery cleans .tmp files</done>
</task>

</tasks>

<verification>
- Logger entry point parses correctly (no syntax errors)
- systemd service has correct After= dependencies
- Integration tests pass: telemetry flows from ZMQ to Parquet to DuckDB query
- Integration tests pass: events flow from ZMQ to JSONL to event query
- Crash recovery works on stale .tmp files
- All tests pass: `uv run pytest src/logger/python/tests/ -x -v`
</verification>

<success_criteria>
- Logger runs as single asyncio process with 4 concurrent tasks
- Full data pipeline verified end-to-end in integration tests
- systemd service file has correct ordering dependencies
- Graceful shutdown flushes all pending writes
- All LOG-01 through LOG-09 requirements covered across Plans 01-06
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-logging/12-06-SUMMARY.md`
</output>
