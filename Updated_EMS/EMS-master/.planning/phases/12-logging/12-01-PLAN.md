---
phase: 12-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger/python/pyproject.toml
  - src/logger/python/src/ems_logger/config.py
  - src/logger/python/src/ems_logger/parquet_schema.py
  - src/common/python/src/ems_common/ipc.py
  - src/common/c/include/ipc_defs.h
  - config/logger_config.yaml
  - config/schemas/logger_config.schema.json
  - src/logger/python/tests/conftest.py
  - src/logger/python/tests/test_parquet_schema.py
autonomous: true
requirements:
  - LOG-08
  - LOG-09

must_haves:
  truths:
    - "Logger config YAML loads and validates against JSON Schema"
    - "Parquet schemas for cluster and system files match RTDB field structure"
    - "SOCK_LOGGER_QUERY constant exists in both Python and C IPC definitions"
    - "pyarrow and duckdb are installable dependencies"
  artifacts:
    - path: "src/logger/python/src/ems_logger/config.py"
      provides: "LoggerConfig dataclass parsed from logger_config.yaml"
      exports: ["LoggerConfig", "load_logger_config"]
    - path: "src/logger/python/src/ems_logger/parquet_schema.py"
      provides: "PyArrow schema builders for cluster and system Parquet files"
      exports: ["build_cluster_schema", "build_system_schema"]
    - path: "config/logger_config.yaml"
      provides: "Logger configuration with retention, compression, query settings"
      contains: "_schema_version"
    - path: "config/schemas/logger_config.schema.json"
      provides: "JSON Schema for logger_config.yaml validation"
    - path: "src/common/python/src/ems_common/ipc.py"
      provides: "SOCK_LOGGER_QUERY constant"
      contains: "SOCK_LOGGER_QUERY"
  key_links:
    - from: "src/logger/python/src/ems_logger/config.py"
      to: "config/logger_config.yaml"
      via: "YAML load + schema validation"
      pattern: "yaml\\.safe_load"
    - from: "src/logger/python/src/ems_logger/parquet_schema.py"
      to: "src/common/python/src/ems_common/rtdb.py"
      via: "Field names match RTDB ctypes struct fields"
      pattern: "pa\\.field"
---

<objective>
Logger foundation: config, dependencies, Parquet schema definitions, and IPC socket constant.

Purpose: All subsequent logger plans depend on config loading, schema definitions, and the pyarrow/duckdb dependencies being available. This plan establishes the contracts that Plans 02-06 implement against.
Output: LoggerConfig dataclass, PyArrow schemas for cluster + system files, logger_config.yaml with JSON Schema, SOCK_LOGGER_QUERY in IPC definitions, test fixtures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-logging/12-CONTEXT.md
@.planning/phases/12-logging/12-RESEARCH.md

@src/common/python/src/ems_common/ipc.py
@src/common/c/include/ipc_defs.h
@src/common/python/src/ems_common/rtdb.py
@src/data_manager/python/src/ems_data_manager/publisher.py
@config/system_config.yaml
@src/logger/python/pyproject.toml

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From src/common/python/src/ems_common/ipc.py:
```python
SOCK_LOGGER: str = "ipc:///run/ems/logger.sock"     # PULL for events
# Need to ADD: SOCK_LOGGER_QUERY for REP query socket
```

From src/data_manager/python/src/ems_data_manager/publisher.py:
```python
# Telemetry topics published at 1Hz:
# "bms.rack.{cluster}.{rack}" -> {pack_v, pack_i, pack_soc, pack_soh, min_cell_v, max_cell_v, avg_cell_v, min_cell_t, max_cell_t, avg_cell_t, fault_code, online}
# "pcs" -> {ac_voltage, ac_current, active_power, reactive_power, dc_voltage, dc_current, frequency, temperature, state, fault_code}
# "gpio" -> {di: [8], do_state: [8]}
# "meter" -> {voltage, current, active_power, reactive_power, frequency, power_factor, energy_import, energy_export}
# "btms" -> {inlet_temp, outlet_temp, fan_speed_pct, cooling_active}
# "system" -> {control_state, source_priority, active_setpoint_kw, total_soc, total_power_kw, total_energy_kwh, ems_uptime_s}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies, IPC constant, and logger config with schema</name>
  <files>
    src/logger/python/pyproject.toml,
    src/common/python/src/ems_common/ipc.py,
    src/common/c/include/ipc_defs.h,
    config/logger_config.yaml,
    config/schemas/logger_config.schema.json,
    src/logger/python/src/ems_logger/config.py
  </files>
  <action>
    1. Add pyarrow and duckdb to logger pyproject.toml dependencies: `uv add pyarrow duckdb` from src/logger/python/. Also add pytest, pytest-asyncio as dev dependencies.

    2. Add SOCK_LOGGER_QUERY to ipc.py:
       ```python
       SOCK_LOGGER_QUERY: str = "ipc:///run/ems/logger_query.sock"
       ```
       Place it after SOCK_LOGGER. Also add to ipc_defs.h as:
       ```c
       #define EMS_SOCK_LOGGER_QUERY "ipc:///run/ems/logger_query.sock"
       ```

    3. Create config/logger_config.yaml with _schema_version: "1.0" and sections per RESEARCH.md:
       - storage: data_dir ("/mnt/ssd/ems/data"), parquet_retention_days (90), jsonl_retention_days (180), disk_usage_threshold_pct (80), cleanup_interval_s (300)
       - parquet: compression ("snappy"), rotation_period_s (3600), row_group_size (3600)
       - query: socket path, per-type max_rows and timeout_s values matching CONTEXT.md table

    4. Create config/schemas/logger_config.schema.json validating the above structure. Follow existing schema patterns (additionalProperties: false at every level, _schema_version as const "1.0").

    5. Create src/logger/python/src/ems_logger/config.py:
       - LoggerConfig Pydantic v2 model (or dataclass -- follow existing pattern in config_manager)
       - load_logger_config(path: Path) -> LoggerConfig function that loads YAML and validates
       - Include all fields from logger_config.yaml with sensible defaults
       - StorageConfig, ParquetConfig, QueryConfig as nested models
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv sync --all-packages && python -c "from ems_logger.config import LoggerConfig, load_logger_config; print('config OK')" && python -c "import pyarrow; import duckdb; print(f'pyarrow {pyarrow.__version__}, duckdb {duckdb.__version__}')"</automated>
  </verify>
  <done>pyarrow and duckdb install successfully; LoggerConfig loads and validates logger_config.yaml; SOCK_LOGGER_QUERY exists in both Python and C IPC definitions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parquet schema builders and test fixtures</name>
  <files>
    src/logger/python/src/ems_logger/parquet_schema.py,
    src/logger/python/tests/conftest.py,
    src/logger/python/tests/test_parquet_schema.py
  </files>
  <behavior>
    - build_cluster_schema(racks=4, modules=8, cells=15, temps=4) returns PyArrow schema with ts + per-rack flat fields + per-module LIST columns
    - build_system_schema() returns PyArrow schema with ts + pcs_* + meter_* + btms_* + gpio_* + system_* columns
    - Cluster schema field count = 1 (ts) + racks * (12 flat + modules * 2 LIST) for default topology
    - System schema has exactly the fields from RESEARCH.md system schema
    - Schemas set compression metadata key to "snappy"
    - build_cluster_schema includes topology metadata dict for Parquet file metadata
  </behavior>
  <action>
    1. Create parquet_schema.py with:
       - build_cluster_schema(racks_per_cluster, modules_per_rack, cells_per_module, temps_per_module) -> pa.Schema
       - build_system_schema() -> pa.Schema
       - build_topology_metadata(cluster_count, racks_per_cluster, modules_per_rack, cells_per_module) -> dict[str, str]
       - Field names must match RTDB telemetry dict keys from publisher.py (rack{N}_pack_v, rack{N}_soc, etc.)
       - Cell voltage LIST columns: rack{N}_mod{M}_cell_v as pa.list_(pa.float32())
       - Cell temperature LIST columns: rack{N}_mod{M}_cell_t as pa.list_(pa.float32())
       - System fields prefixed: pcs_ac_voltage, meter_voltage, btms_inlet_temp, gpio_di, gpio_do, system_control_state, etc.

    2. Create tests/conftest.py with shared fixtures:
       - tmp_data_dir fixture (tmp_path based)
       - sample_rack_telemetry() -> dict matching publisher rack fields
       - sample_system_telemetry() -> dict matching publisher system/pcs/gpio/meter/btms fields
       - sample_event() -> dict matching event envelope fields
       - default_topology fixture (cluster_count=1, racks_per_cluster=4, modules_per_rack=8, cells_per_module=15, temps_per_module=4)

    3. Create tests/test_parquet_schema.py with tests for both schemas, field counts, LIST column types, and metadata.
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv run pytest src/logger/python/tests/test_parquet_schema.py -x -v</automated>
  </verify>
  <done>Cluster and system Parquet schemas build correctly with proper field types; test fixtures provide reusable sample data for all subsequent plans</done>
</task>

</tasks>

<verification>
- `uv sync --all-packages` succeeds with pyarrow and duckdb
- `from ems_logger.config import load_logger_config` works
- `from ems_logger.parquet_schema import build_cluster_schema, build_system_schema` works
- All tests in test_parquet_schema.py pass
- SOCK_LOGGER_QUERY appears in ipc.py
</verification>

<success_criteria>
- pyarrow and duckdb are importable after uv sync
- LoggerConfig parses logger_config.yaml correctly
- Cluster schema has ts + rack flat fields + module LIST columns
- System schema has ts + all section-prefixed flat fields + GPIO array columns
- SOCK_LOGGER_QUERY defined in both Python and C IPC files
- Test fixtures produce valid sample data dicts
</success_criteria>

<output>
After completion, create `.planning/phases/12-logging/12-01-SUMMARY.md`
</output>
