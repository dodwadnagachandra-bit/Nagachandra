---
phase: 27-yocto-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - yocto/meta-ems/recipes-core/images/core-image-ems.bb
  - yocto/meta-ems/recipes-core/base-files/base-files_%.bbappend
  - deploy/tmpfiles/ems.conf
  - tests/test_yocto_recipes.py
autonomous: true
requirements:
  - PROD-01
  - PROD-02

must_haves:
  truths:
    - "Image recipe enables read-only-rootfs with tmpfs overlays for /tmp, /var/run, /run/ems, /var/log/journal"
    - "SSD data partition mounted read-write at /data"
    - "IMAGE_INSTALL includes ems-boot-script and ems-certs (created by plans 27-03 and 27-04; bitbake resolves at build time)"
    - "IMAGE_BOOT_FILES includes boot.scr for U-Boot A/B bootscript"
    - "ROOTFS_POSTPROCESS_COMMAND creates Python venv from staged wheels"
  artifacts:
    - path: "yocto/meta-ems/recipes-core/images/core-image-ems.bb"
      provides: "EMS production image recipe"
      contains: "read-only-rootfs"
    - path: "yocto/meta-ems/recipes-core/base-files/base-files_%.bbappend"
      provides: "fstab with tmpfs and SSD mounts"
      contains: "tmpfs"
    - path: "tests/test_yocto_recipes.py"
      provides: "Recipe file validation tests"
      min_lines: 50
  key_links:
    - from: "yocto/meta-ems/recipes-core/images/core-image-ems.bb"
      to: "All recipes-ems packages"
      via: "IMAGE_INSTALL:append"
      pattern: "IMAGE_INSTALL.*safety-manager"
    - from: "yocto/meta-ems/recipes-core/images/core-image-ems.bb"
      to: "ems-boot-script recipe"
      via: "IMAGE_INSTALL:append and IMAGE_BOOT_FILES"
      pattern: "ems-boot-script"
    - from: "yocto/meta-ems/recipes-core/images/core-image-ems.bb"
      to: "ems-certs recipe"
      via: "IMAGE_INSTALL:append"
      pattern: "ems-certs"
    - from: "yocto/meta-ems/recipes-core/images/core-image-ems.bb"
      to: "boot.scr on boot partition"
      via: "IMAGE_BOOT_FILES:append"
      pattern: "IMAGE_BOOT_FILES.*boot\\.scr"
    - from: "yocto/meta-ems/recipes-core/base-files/base-files_%.bbappend"
      to: "/etc/fstab"
      via: "do_install:append fstab entries"
      pattern: "tmpfs.*run/ems"
---

<objective>
Create the EMS production image recipe with read-only rootfs, fstab overlays for tmpfs and SSD data partition, and Python venv creation via ROOTFS_POSTPROCESS_COMMAND. Includes validation tests for all Yocto recipe files.

Purpose: The image recipe wires all individual module recipes into a deployable production image with the correct filesystem layout.
Output: core-image-ems.bb, base-files bbappend, tmpfiles config, and validation tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-yocto-migration/27-CONTEXT.md
@.planning/phases/27-yocto-migration/27-RESEARCH.md
@.planning/phases/27-yocto-migration/27-VALIDATION.md

@cmake/toolchains/aarch64-linux.cmake
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Image recipe with read-only rootfs, fstab overlays, and validation tests</name>
  <files>
    yocto/meta-ems/recipes-core/images/core-image-ems.bb
    yocto/meta-ems/recipes-core/base-files/base-files_%.bbappend
    deploy/tmpfiles/ems.conf
    tests/test_yocto_recipes.py
  </files>
  <behavior>
    - test_layer_conf_exists: layer.conf file exists at yocto/meta-ems/conf/layer.conf
    - test_layer_conf_scarthgap: layer.conf contains LAYERSERIES_COMPAT with "scarthgap"
    - test_layer_conf_dependencies: layer.conf has LAYERDEPENDS with meta-ti-bsp, meta-oe, meta-python
    - test_all_c_recipes_exist: All 5 C binary .bb files exist in recipes-ems/
    - test_image_recipe_readonly: core-image-ems.bb contains "read-only-rootfs" in IMAGE_FEATURES
    - test_image_recipe_python_deps: core-image-ems.bb installs python3, python3-modules, python3-venv
    - test_image_recipe_venv_postprocess: core-image-ems.bb has ROOTFS_POSTPROCESS_COMMAND with venv creation
    - test_image_recipe_boot_script: core-image-ems.bb includes ems-boot-script in IMAGE_INSTALL
    - test_image_recipe_certs: core-image-ems.bb includes ems-certs in IMAGE_INSTALL
    - test_image_recipe_boot_files: core-image-ems.bb includes boot.scr in IMAGE_BOOT_FILES
    - test_fstab_tmpfs_tmp: base-files bbappend has tmpfs entry for /tmp (50m)
    - test_fstab_tmpfs_var_run: base-files bbappend has tmpfs entry for /var/run (10m)
    - test_fstab_tmpfs_run_ems: base-files bbappend has tmpfs entry for /run/ems (5m)
    - test_fstab_tmpfs_journal: base-files bbappend has tmpfs entry for /var/log/journal (50m)
    - test_fstab_ssd_data: base-files bbappend has /dev/nvme0n1p1 mounted at /data as ext4 rw
    - test_cmake_sysroot_comment: aarch64-linux.cmake has commented CMAKE_SYSROOT line (for Yocto SDK)
    - test_systemd_units_recipe_exists: ems-systemd-units recipe exists
    - test_frontend_recipe_exists: ems-frontend recipe installs to /opt/ems/frontend/dist/
  </behavior>
  <action>
    **core-image-ems.bb (image recipe):**
    - `require recipes-core/images/core-image-minimal.bb`
    - DESCRIPTION = "EMS production image for Advantech ECU-1170-552A"
    - IMAGE_FEATURES += "read-only-rootfs ssh-server-openssh"
    - IMAGE_INSTALL:append with ALL packages including those from other plans (bitbake resolves recipe dependencies at build time, not at parse time for IMAGE_INSTALL lists):
      python3, python3-modules, python3-venv, python3-pip, python3-wheel,
      ems-common-c, safety-manager, comm-manager-c, data-manager-c, control-manager-c,
      ems-python-venv, ems-config, ems-systemd-units, ems-frontend,
      libubootenv, libubootenv-bin,
      ems-boot-script, ems-certs
    - IMAGE_BOOT_FILES:append = " boot.scr" (compiled U-Boot script from ems-boot-script recipe)
    - DISTRO_FEATURES:append = " systemd"
    - VIRTUAL-RUNTIME_init_manager = "systemd"
    - ROOTFS_POSTPROCESS_COMMAND += "create_ems_venv; "
    - create_ems_venv() function that:
      1. Creates venv: `python3 -m venv --without-pip ${IMAGE_ROOTFS}/opt/ems/python/.venv`
      2. Installs from wheels: pip install --no-index --find-links=.../wheels -r requirements.lock
      3. Cleans up pip from venv to reduce size

    **base-files bbappend:**
    - do_install:append() that adds to ${D}${sysconfdir}/fstab:
      - tmpfs /tmp (50m, mode=1777)
      - tmpfs /var/run (10m, mode=0755)
      - tmpfs /run/ems (5m, mode=0755)
      - tmpfs /var/log/journal (50m, mode=0755)
      - /dev/nvme0n1p1 /data ext4 defaults,noatime 0 2

    **deploy/tmpfiles/ems.conf (systemd-tmpfiles):**
    - d /run/ems 0755 ems ems -
    - d /data/config 0755 ems ems -
    - d /data/parquet 0755 ems ems -
    - d /data/jsonl 0755 ems ems -
    - d /data/cloud_buffer 0755 ems ems -
    - d /data/rtdb_snapshots 0755 ems ems -

    **tests/test_yocto_recipes.py:**
    - Write pytest tests that validate:
      1. File existence of all recipe files (layer.conf, all .bb files, bbappend)
      2. layer.conf content (LAYERSERIES_COMPAT, LAYERDEPENDS)
      3. Image recipe content (read-only-rootfs, IMAGE_INSTALL packages including ems-boot-script and ems-certs, IMAGE_BOOT_FILES with boot.scr, ROOTFS_POSTPROCESS_COMMAND)
      4. fstab content in base-files bbappend (all tmpfs entries + SSD data partition)
      5. C recipes contain "inherit cmake externalsrc"
      6. cmake/toolchains/aarch64-linux.cmake still has commented CMAKE_SYSROOT
    - Use pathlib to find the repo root (same _find_repo_root pattern as diagnostics tests)
    - All tests are pure file-content checks — no bitbake execution needed
  </action>
  <verify>
    <automated>uv run pytest tests/test_yocto_recipes.py -x -q</automated>
  </verify>
  <done>
    - core-image-ems.bb enables read-only-rootfs and includes ALL EMS packages (including ems-boot-script and ems-certs)
    - IMAGE_BOOT_FILES includes boot.scr
    - base-files bbappend adds all 5 fstab entries (4 tmpfs + 1 SSD data)
    - deploy/tmpfiles/ems.conf creates runtime directories
    - All tests in test_yocto_recipes.py pass
  </done>
</task>

</tasks>

<verification>
- Image recipe has read-only-rootfs + all packages (including ems-boot-script, ems-certs) + venv postprocess
- IMAGE_BOOT_FILES includes boot.scr
- fstab has all required mount points per CONTEXT.md
- `uv run pytest tests/test_yocto_recipes.py -x -q` passes
</verification>

<success_criteria>
- Read-only rootfs with tmpfs overlays for /tmp, /var/run, /run/ems, /var/log/journal
- SSD data partition at /data
- All module packages in IMAGE_INSTALL (including ems-boot-script and ems-certs from plans 27-03/27-04)
- boot.scr in IMAGE_BOOT_FILES
- Validation tests confirm all recipe files and content
</success_criteria>

<output>
After completion, create `.planning/phases/27-yocto-migration/27-02-SUMMARY.md`
</output>
