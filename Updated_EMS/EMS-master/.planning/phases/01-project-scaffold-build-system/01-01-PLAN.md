---
phase: 01-project-scaffold-build-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .clang-format
  - CMakeLists.txt
  - Makefile
  - pyproject.toml
  - cmake/toolchains/aarch64-linux.cmake
  - src/common/c/CMakeLists.txt
  - src/common/c/include/ems_types.h
  - src/common/python/pyproject.toml
  - src/common/python/src/ems_common/__init__.py
  - src/safety_manager/CMakeLists.txt
  - src/safety_manager/src/main.c
  - src/comm_manager/c/CMakeLists.txt
  - src/comm_manager/c/src/main.c
  - src/comm_manager/python/pyproject.toml
  - src/comm_manager/python/src/ems_comm_manager/__init__.py
  - src/data_manager/c/CMakeLists.txt
  - src/data_manager/c/src/main.c
  - src/data_manager/python/pyproject.toml
  - src/data_manager/python/src/ems_data_manager/__init__.py
  - src/logger/c/CMakeLists.txt
  - src/logger/c/src/main.c
  - src/logger/python/pyproject.toml
  - src/logger/python/src/ems_logger/__init__.py
  - src/config_manager/pyproject.toml
  - src/config_manager/src/ems_config_manager/__init__.py
  - src/control_manager/c/CMakeLists.txt
  - src/control_manager/c/src/main.c
  - src/control_manager/python/pyproject.toml
  - src/control_manager/python/src/ems_control_manager/__init__.py
  - src/alarm_manager/pyproject.toml
  - src/alarm_manager/src/ems_alarm_manager/__init__.py
  - src/scheduler/pyproject.toml
  - src/scheduler/src/ems_scheduler/__init__.py
  - src/diagnostics/pyproject.toml
  - src/diagnostics/src/ems_diagnostics/__init__.py
  - src/cloud_manager/pyproject.toml
  - src/cloud_manager/src/ems_cloud_manager/__init__.py
  - src/ota_manager/pyproject.toml
  - src/ota_manager/src/ems_ota_manager/__init__.py
  - src/hmi_server/pyproject.toml
  - src/hmi_server/src/ems_hmi_server/__init__.py
  - src/hmi_server/frontend/package.json
  - src/hmi_server/frontend/tsconfig.json
  - src/hmi_server/frontend/vite.config.ts
  - src/hmi_server/frontend/index.html
  - src/hmi_server/frontend/src/main.tsx
  - src/hmi_server/frontend/src/App.tsx
  - config/.gitkeep
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_scaffold.py
  - docs/.gitkeep
  - tools/simulators/.gitkeep
autonomous: true
requirements:
  - PLAT-03
must_haves:
  truths:
    - "All 12 module directories exist under src/ with correct structure (C-only, Python-only, or hybrid c/+python/ subdirs)"
    - "CMake configure succeeds and builds a C executable from a stub main.c (native build)"
    - "uv sync succeeds and installs all 12 Python workspace members into .venv"
    - "bun install succeeds in src/hmi_server/frontend/"
    - "pytest discovers and runs at least one test from tests/"
    - "make build and make test both succeed end-to-end"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Top-level CMake project with add_subdirectory for all C modules"
      contains: "add_subdirectory(src/safety_manager)"
    - path: "pyproject.toml"
      provides: "uv workspace root with all 12 Python members"
      contains: "[tool.uv.workspace]"
    - path: "Makefile"
      provides: "Developer interface: setup, build, build-arm, test, lint, fmt, sim, flash, clean"
      contains: "make build"
    - path: ".clang-format"
      provides: "Allman-style C formatting config"
      contains: "BreakBeforeBraces: Allman"
    - path: ".gitignore"
      provides: "Ignore rules for build/, .venv/, node_modules/, __pycache__/"
      contains: "build/"
    - path: "src/safety_manager/src/main.c"
      provides: "Stub C entry point for safety_manager"
    - path: "tests/test_scaffold.py"
      provides: "Smoke test verifying module structure"
  key_links:
    - from: "CMakeLists.txt"
      to: "src/common/c/CMakeLists.txt"
      via: "add_subdirectory(src/common/c)"
      pattern: "add_subdirectory.*src/common/c"
    - from: "CMakeLists.txt"
      to: "src/safety_manager/CMakeLists.txt"
      via: "add_subdirectory(src/safety_manager)"
      pattern: "add_subdirectory.*src/safety_manager"
    - from: "pyproject.toml"
      to: "src/common/python/pyproject.toml"
      via: "uv workspace members list"
      pattern: "src/common/python"
    - from: "Makefile"
      to: "CMakeLists.txt"
      via: "cmake -B build"
      pattern: "cmake -B"
    - from: "Makefile"
      to: "pyproject.toml"
      via: "uv sync"
      pattern: "uv sync"
---

<objective>
Create the complete EMS monorepo directory structure, all root configuration files (CMakeLists.txt, pyproject.toml, Makefile, .clang-format, .gitignore), and stub files for all 12 modules. This is the foundational scaffold that every subsequent phase builds upon.

Purpose: Establish the monorepo structure so that `make build` and `make test` succeed, uv workspace resolves all Python members, CMake compiles C stubs, and the HMI frontend initializes. No module logic — only the skeleton.

Output: A fully buildable monorepo scaffold with 12 module directories, build system configs, and a passing smoke test.

**Executor note:** This plan touches 56 files — high count but low per-file complexity (boilerplate stubs). Use the category-based action descriptions (A-F in Task 2) as templates and proceed methodically. Prioritize consistency across the 12 module stubs (identical structure, naming conventions, version strings).
</objective>

<execution_context>
@/home/overlord/.claude/get-shit-done/workflows/execute-plan.md
@/home/overlord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-build-system/01-CONTEXT.md
@.planning/phases/01-project-scaffold-build-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root configuration files and directory skeleton</name>
  <files>
    .gitignore
    .clang-format
    CMakeLists.txt
    pyproject.toml
    Makefile
    cmake/toolchains/aarch64-linux.cmake
    config/.gitkeep
    tests/__init__.py
    tests/conftest.py
    docs/.gitkeep
    tools/simulators/.gitkeep
  </files>
  <action>
    Create the root-level configuration files that define the build system, formatting, and developer workflow:

    1. **.gitignore** — Ignore patterns for: `build/`, `build-arm/`, `.venv/`, `node_modules/`, `__pycache__/`, `*.o`, `*.elf`, `*.so`, `*.pyc`, `.eggs/`, `dist/`, `*.egg-info/`, `.ruff_cache/`, `uv.lock` (do NOT ignore — commit it), `bun.lockb`. Also ignore IDE dirs: `.idea/`, `.vscode/`, `*.swp`.

    2. **.clang-format** — Allman brace style per global CLAUDE.md preferences:
       ```yaml
       BasedOnStyle: LLVM
       BreakBeforeBraces: Allman
       IndentWidth: 4
       ColumnLimit: 100
       AlignConsecutiveAssignments: true
       AlignConsecutiveDeclarations: true
       SortIncludes: true
       ```

    3. **CMakeLists.txt** (root) — Follow Pattern 2 from RESEARCH.md exactly:
       - `cmake_minimum_required(VERSION 3.22)` (Ubuntu 22.04 apt default)
       - `project(EMS C CXX)`
       - C99 standard, C++17 standard, both REQUIRED
       - `include(CTest)` before any add_subdirectory
       - add_subdirectory for: src/common/c, src/safety_manager, src/comm_manager/c, src/data_manager/c, src/logger/c, src/control_manager/c
       - Guard test targets with `if(NOT CMAKE_CROSSCOMPILING)` at the bottom (for future test subdirectories)

    4. **pyproject.toml** (root) — Follow Pattern 3 from RESEARCH.md exactly:
       - `[project]` with name="ems", version="0.1.0", requires-python=">=3.12"
       - `[tool.uv.workspace]` with explicit members list (all 12 Python paths as specified in RESEARCH.md)
       - `[dependency-groups]` dev group with pytest>=8.0, ruff>=0.9
       - `[build-system]` with uv_build backend
       - `[tool.ruff]` config: line-length=100, target-version="py312", exclude patterns
       - `[tool.ruff.lint]` select=["E","F","I","N","W","B"], ignore=["E501"]
       - `[tool.ruff.format]` quote-style="double", indent-style="space"
       - `[tool.pytest.ini_options]` testpaths=["tests"], python_files=["test_*.py"]

    5. **Makefile** — Follow Pattern 7 from RESEARCH.md. All targets from CONTEXT.md locked decisions:
       - `help`, `setup`, `build`, `build-arm`, `test`, `lint`, `fmt`, `sim`, `build-hmi`, `dev-hmi`, `test-hmi`, `flash`, `clean`
       - `BUILD_DIR := build`, `BUILD_ARM := build-arm`, `ECU_HOST ?= ems@192.168.1.100`
       - `setup` target: install system deps via apt, install Python 3.12 via `uv python install 3.12`, run `uv sync`, run `bun install` in frontend dir
       - `build` target: cmake -B build -DCMAKE_BUILD_TYPE=Debug, cmake --build build -j$(nproc)
       - `build-arm` target: cmake with toolchain file, Release mode
       - `test` target: ctest + uv run pytest
       - `lint` target: clang-format --dry-run --Werror + ruff check + ruff format --check
       - `fmt` target: clang-format -i + ruff format + ruff check --fix
       - All .PHONY declarations

    6. **cmake/toolchains/aarch64-linux.cmake** — Follow Pattern 1 from RESEARCH.md exactly:
       - CMAKE_SYSTEM_NAME Linux, CMAKE_SYSTEM_PROCESSOR aarch64
       - CMAKE_C_COMPILER /usr/bin/aarch64-linux-gnu-gcc
       - CMAKE_CXX_COMPILER /usr/bin/aarch64-linux-gnu-g++
       - FIND_ROOT_PATH_MODE settings (NEVER for PROGRAM, ONLY for LIBRARY/INCLUDE/PACKAGE)

    7. Create empty directories with .gitkeep: config/, docs/, tools/simulators/

    8. **tests/__init__.py** — empty file
    9. **tests/conftest.py** — minimal conftest with a docstring: `"""EMS integration test fixtures."""`
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && test -f .gitignore && test -f .clang-format && test -f CMakeLists.txt && test -f pyproject.toml && test -f Makefile && test -f cmake/toolchains/aarch64-linux.cmake && test -f tests/conftest.py && echo "PASS: All root config files exist"</automated>
  </verify>
  <done>All root configuration files exist: .gitignore, .clang-format, CMakeLists.txt, pyproject.toml, Makefile, cmake/toolchains/aarch64-linux.cmake. Empty placeholder directories for config/, docs/, tools/simulators/, tests/ are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold all 12 module directories with stub source files</name>
  <files>
    src/common/c/CMakeLists.txt
    src/common/c/include/ems_types.h
    src/common/python/pyproject.toml
    src/common/python/src/ems_common/__init__.py
    src/safety_manager/CMakeLists.txt
    src/safety_manager/src/main.c
    src/comm_manager/c/CMakeLists.txt
    src/comm_manager/c/src/main.c
    src/comm_manager/python/pyproject.toml
    src/comm_manager/python/src/ems_comm_manager/__init__.py
    src/data_manager/c/CMakeLists.txt
    src/data_manager/c/src/main.c
    src/data_manager/python/pyproject.toml
    src/data_manager/python/src/ems_data_manager/__init__.py
    src/logger/c/CMakeLists.txt
    src/logger/c/src/main.c
    src/logger/python/pyproject.toml
    src/logger/python/src/ems_logger/__init__.py
    src/config_manager/pyproject.toml
    src/config_manager/src/ems_config_manager/__init__.py
    src/control_manager/c/CMakeLists.txt
    src/control_manager/c/src/main.c
    src/control_manager/python/pyproject.toml
    src/control_manager/python/src/ems_control_manager/__init__.py
    src/alarm_manager/pyproject.toml
    src/alarm_manager/src/ems_alarm_manager/__init__.py
    src/scheduler/pyproject.toml
    src/scheduler/src/ems_scheduler/__init__.py
    src/diagnostics/pyproject.toml
    src/diagnostics/src/ems_diagnostics/__init__.py
    src/cloud_manager/pyproject.toml
    src/cloud_manager/src/ems_cloud_manager/__init__.py
    src/ota_manager/pyproject.toml
    src/ota_manager/src/ems_ota_manager/__init__.py
    src/hmi_server/pyproject.toml
    src/hmi_server/src/ems_hmi_server/__init__.py
    src/hmi_server/frontend/package.json
    src/hmi_server/frontend/tsconfig.json
    src/hmi_server/frontend/vite.config.ts
    src/hmi_server/frontend/index.html
    src/hmi_server/frontend/src/main.tsx
    src/hmi_server/frontend/src/App.tsx
  </files>
  <action>
    Create all 12 module directories with appropriate stub files. Modules fall into three categories:

    **A. Shared common library (src/common/)**

    1. **src/common/c/CMakeLists.txt** — INTERFACE library `ems_common_c` that exposes include/ directory:
       ```cmake
       add_library(ems_common_c INTERFACE)
       target_include_directories(ems_common_c INTERFACE ${CMAKE_CURRENT_SOURCE_DIR}/include)
       ```

    2. **src/common/c/include/ems_types.h** — Stub header with include guard, a version macro `#define EMS_VERSION "0.1.0"`, and a placeholder comment for future types (rtdb, seqlock, ipc_topics will be added in later phases).

    3. **src/common/python/pyproject.toml** — Package `ems-common`, version 0.1.0, requires-python>=3.12, no dependencies. Build backend: uv_build.
    4. **src/common/python/src/ems_common/__init__.py** — `"""EMS common Python library."""` with `__version__ = "0.1.0"`.

    **B. C-only module: safety_manager**

    5. **src/safety_manager/CMakeLists.txt** — Following Pattern from RESEARCH.md:
       - `add_executable(safety_manager src/main.c)`
       - `target_link_libraries(safety_manager PRIVATE ems_common_c)`
       - `target_compile_options(safety_manager PRIVATE -Wall -Wextra -Werror` with Debug/Release generator expressions`)`
       - Guard `if(NOT CMAKE_CROSSCOMPILING)` for future test subdirectory

    6. **src/safety_manager/src/main.c** — Minimal C stub:
       ```c
       #include "ems_types.h"
       #include <stdio.h>

       int main(void)
       {
           printf("EMS Safety Manager %s\n", EMS_VERSION);
           return 0;
       }
       ```
       Uses Allman brace style. Includes ems_types.h to verify the common library link works.

    **C. Hybrid modules (C + Python): comm_manager, data_manager, logger, control_manager**

    For each hybrid module, create:
    - `{module}/c/CMakeLists.txt` — `add_executable({module_name}_c src/main.c)`, links ems_common_c, same compile options as safety_manager
    - `{module}/c/src/main.c` — Stub C main printing module name and version
    - `{module}/python/pyproject.toml` — Package `ems-{module-name}`, version 0.1.0, requires-python>=3.12, depends on `ems-common` (workspace source). Build backend: uv_build.
    - `{module}/python/src/ems_{module_name}/__init__.py` — Docstring + `__version__ = "0.1.0"`

    Module executable names for CMake: `comm_manager_c`, `data_manager_c`, `logger_c`, `control_manager_c` (avoid collision with Python packages).

    **D. Python-only modules: config_manager, alarm_manager, scheduler, diagnostics, cloud_manager, ota_manager, hmi_server**

    For each Python-only module, create:
    - `{module}/pyproject.toml` — Package `ems-{module-name}`, version 0.1.0, requires-python>=3.12, depends on `ems-common` (workspace source). Build backend: uv_build.
    - `{module}/src/ems_{module_name}/__init__.py` — Docstring + `__version__ = "0.1.0"`

    Use hyphens in package names (e.g., `ems-config-manager`) and underscores in import names (e.g., `ems_config_manager`).

    **E. HMI frontend (src/hmi_server/frontend/)**

    Create a minimal React + Vite + TypeScript scaffold:
    - **package.json** — name: "ems-hmi", private: true, scripts: {dev: "vite", build: "vite build", preview: "vite preview", test: "echo 'no tests yet'"}, dependencies: {react: "^19.0.0", react-dom: "^19.0.0"}, devDependencies: {vite: "^6.0.0", "@vitejs/plugin-react": "^4.0.0", typescript: "^5.0.0", "@types/react": "^19.0.0", "@types/react-dom": "^19.0.0", tailwindcss: "^4.0.0", "@tailwindcss/vite": "^4.0.0"}
    - **tsconfig.json** — Standard React TS config with strict mode, jsx: "react-jsx", module: "ESNext"
    - **vite.config.ts** — Import react plugin and tailwind vite plugin, define config with plugins: [tailwindCSS(), react()]
    - **index.html** — Minimal HTML5 with div#root and script type="module" src="/src/main.tsx"
    - **src/main.tsx** — ReactDOM.createRoot, renders App component
    - **src/App.tsx** — Simple functional component returning "EMS HMI" heading

    **F. Smoke test (tests/test_scaffold.py)**

    Create `tests/test_scaffold.py` with tests that verify the module structure:
    - `test_all_module_dirs_exist()` — Assert all 12 module directories exist under src/
    - `test_common_python_importable()` — `import ems_common` succeeds
    - `test_python_modules_importable()` — Import each of the 12 Python packages (ems_common, ems_config_manager, ems_alarm_manager, ems_scheduler, ems_diagnostics, ems_cloud_manager, ems_ota_manager, ems_hmi_server, ems_comm_manager, ems_data_manager, ems_logger, ems_control_manager)
    - `test_version_strings()` — Each package has `__version__ == "0.1.0"`
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && cmake -B build -DCMAKE_BUILD_TYPE=Debug 2>&1 | tail -5 && cmake --build build -- -j$(nproc) 2>&1 | tail -5 && uv sync --dev 2>&1 | tail -5 && uv run pytest tests/test_scaffold.py -v 2>&1 | tail -20 && echo "PASS: Build + test succeeded"</automated>
  </verify>
  <done>All 12 module directories exist with correct structure. CMake configures and builds all C executables (safety_manager, comm_manager_c, data_manager_c, logger_c, control_manager_c). uv sync resolves all 12 Python workspace members. pytest discovers and passes all scaffold smoke tests including Python package imports. bun install succeeds in src/hmi_server/frontend/.</done>
</task>

</tasks>

<verification>
1. `cmake -B build && cmake --build build` — All C targets compile without errors
2. `uv sync --dev` — All Python workspace members resolve, .venv created
3. `uv run pytest tests/ -v` — Scaffold smoke tests pass (module dirs, imports, versions)
4. `cd src/hmi_server/frontend && bun install` — HMI dependencies install
5. `./build/safety_manager` — Prints version string and exits 0
6. `make build && make test` — End-to-end developer workflow succeeds
7. Directory count: `ls -d src/*/` shows exactly 13 entries (12 modules + common)
</verification>

<success_criteria>
- `make build` succeeds (CMake configure + compile of 5 C executables)
- `make test` succeeds (ctest finds 0 C tests [stubs only], pytest passes scaffold tests)
- `uv sync --dev` resolves all 12 workspace members
- `bun install` succeeds in frontend directory
- All 12 module directories match the CONTEXT.md layout specification
- .clang-format enforces Allman style
- .gitignore covers build/, .venv/, node_modules/, __pycache__/
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-build-system/01-01-SUMMARY.md`
</output>
