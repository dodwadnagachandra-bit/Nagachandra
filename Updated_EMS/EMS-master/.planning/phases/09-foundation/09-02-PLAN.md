---
phase: 09-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data_manager/c/include/rtdb_lifecycle.h
  - src/data_manager/c/src/rtdb_lifecycle.c
  - src/data_manager/c/src/main.c
  - src/data_manager/c/CMakeLists.txt
  - src/common/python/src/ems_common/rtdb.py
  - tests/test_data_manager.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "RTDB shared memory segment is created with correct size (sizeof ems_rtdb_t) via shm_open + ftruncate + mmap"
    - "RTDB is zero-filled and initialized with magic, version, and topology counts from system_config"
    - "C library (libems_rtdb.so) provides create/attach/detach/destroy API"
    - "Python can attach to C-created shm, read magic/version/topology, and detach without resource_tracker issues"
    - "Stale shm from previous crash is detected and recreated"
    - "Topology counts in RTDB header match system_config values"
  artifacts:
    - path: "src/data_manager/c/include/rtdb_lifecycle.h"
      provides: "Public C API: rtdb_create, rtdb_attach, rtdb_detach, rtdb_destroy"
      exports: ["rtdb_create", "rtdb_attach", "rtdb_detach", "rtdb_destroy"]
    - path: "src/data_manager/c/src/rtdb_lifecycle.c"
      provides: "C implementation of shm lifecycle with stale detection"
      min_lines: 80
    - path: "src/data_manager/c/src/main.c"
      provides: "data_manager_c executable that creates RTDB and holds it open"
      min_lines: 40
    - path: "tests/test_data_manager.py"
      provides: "Unit/integration tests for DATA-01 through DATA-04"
      min_lines: 60
  key_links:
    - from: "src/data_manager/c/src/rtdb_lifecycle.c"
      to: "src/common/c/include/rtdb.h"
      via: "#include rtdb.h"
      pattern: "include.*rtdb\\.h"
    - from: "src/data_manager/c/src/rtdb_lifecycle.c"
      to: "/dev/shm/ems_rtdb"
      via: "shm_open + mmap"
      pattern: "shm_open"
    - from: "src/common/python/src/ems_common/rtdb.py"
      to: "/dev/shm/ems_rtdb"
      via: "SharedMemory attach + resource_tracker.unregister"
      pattern: "resource_tracker\\.unregister"
---

<objective>
Build RTDB shared memory lifecycle in C: create/attach/detach/destroy API as libems_rtdb.so, and the data_manager_c executable that owns the shm segment. Extend Python rtdb.py with attach/detach helpers.

Purpose: RTDB is the real-time data backbone. Every module reads or writes it. The C process owns the shm segment; all consumers (Python and C) attach via the library API.
Output: libems_rtdb.so shared library, data_manager_c executable, Python attach helpers, test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-foundation/09-CONTEXT.md
@.planning/phases/09-foundation/09-RESEARCH.md

@src/common/c/include/rtdb.h
@src/common/c/include/seqlock.h
@src/common/c/include/ipc_defs.h
@src/common/python/src/ems_common/rtdb.py
@src/data_manager/c/CMakeLists.txt
@src/data_manager/c/src/main.c

<interfaces>
From src/common/c/include/rtdb.h:
```c
#define RTDB_VERSION          1
#define RTDB_MAGIC            0x454D5352  /* "EMSR" */
#define MAX_CLUSTERS          8
#define MAX_RACKS_PER_CLUSTER 16

typedef struct { ... } ems_rtdb_t;  /* sizeof = 1,800,744 bytes */

/* Header fields: magic, version, cluster_count, racks_per_cluster,
   modules_per_rack, cells_per_module, temps_per_module */
/* Sections: clusters[], pcs, gpio, meter, btms, system -- each with seqlock */
```

From src/common/c/include/seqlock.h:
```c
static inline void ems_seqlock_init(ems_seqlock_t *lock);
static inline uint32_t ems_seqlock_read_begin(const ems_seqlock_t *lock);
static inline bool ems_seqlock_read_retry(const ems_seqlock_t *lock, uint32_t seq);
static inline void ems_seqlock_write_begin(ems_seqlock_t *lock);
static inline void ems_seqlock_write_end(ems_seqlock_t *lock);
```

From src/common/python/src/ems_common/rtdb.py:
```python
class EmsRtdb(ctypes.Structure):
    """Full ctypes mirror of ems_rtdb_t."""
RTDB_MAGIC = 0x454D5352
RTDB_VERSION = 1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: libems_rtdb.so C lifecycle library and data_manager_c executable</name>
  <files>
    src/data_manager/c/include/rtdb_lifecycle.h,
    src/data_manager/c/src/rtdb_lifecycle.c,
    src/data_manager/c/src/main.c,
    src/data_manager/c/CMakeLists.txt
  </files>
  <behavior>
    - Test: rtdb_create produces shm at /dev/shm/ems_rtdb with sizeof(ems_rtdb_t) bytes
    - Test: Created RTDB has correct magic (0x454D5352) and version (1)
    - Test: Created RTDB has topology counts matching input parameters
    - Test: All seqlocks initialized to 0 (even = no write in progress)
    - Test: rtdb_attach on existing shm returns valid pointer with correct magic
    - Test: rtdb_detach unmaps without unlinking
    - Test: rtdb_destroy unlinks shm
    - Test: Stale shm detection: if shm exists with wrong magic/version, destroy and recreate
    - Test: data_manager_c creates RTDB and holds it open until SIGTERM
  </behavior>
  <action>
  1. Create `src/data_manager/c/include/rtdb_lifecycle.h`:
     ```
     ems_rtdb_t *rtdb_create(uint8_t clusters, uint8_t racks_per_cluster,
                              uint8_t modules_per_rack, uint8_t cells_per_module,
                              uint8_t temps_per_module);
     ems_rtdb_t *rtdb_attach(void);
     void rtdb_detach(ems_rtdb_t *rtdb);
     void rtdb_destroy(void);
     ```
     Define `RTDB_SHM_NAME "/ems_rtdb"`.

  2. Create `src/data_manager/c/src/rtdb_lifecycle.c`:
     - `rtdb_create()`: Check for stale shm first (shm_open O_RDWR, if exists: mmap, check magic/version, if mismatch or any error: close + shm_unlink). Then shm_open O_CREAT|O_EXCL|O_RDWR, ftruncate to sizeof(ems_rtdb_t), mmap MAP_SHARED. memset(0, sizeof). Write magic, version, topology counts. Initialize all seqlocks via ems_seqlock_init (per-rack in each cluster + pcs + gpio + meter + btms + system). Per locked decision: RTDB zero-filled on creation, stale shm detected and recreated.
     - `rtdb_attach()`: shm_open O_RDWR (not O_CREAT), mmap, verify magic+version. Return NULL on failure.
     - `rtdb_detach()`: munmap only, do NOT shm_unlink.
     - `rtdb_destroy()`: shm_unlink. Per locked decision: graceful shutdown unlinks.
     - Use `close(fd)` after mmap in all cases (fd not needed once mapped).

  3. Update `src/data_manager/c/src/main.c`:
     - Parse topology from command-line args (or read from YAML -- simplest: pass as CLI args from systemd ExecStart, e.g., `data_manager_c 1 4 10 16 8`).
     - Call rtdb_create() with topology values.
     - Install SIGTERM/SIGINT handler that sets a shutdown flag.
     - Block in a loop (pause() or sigwait) until shutdown signal.
     - On shutdown: rtdb_detach, rtdb_destroy, exit 0.
     - Print "RTDB created: {size} bytes, {clusters}c/{racks}r" on success.

  4. Update `src/data_manager/c/CMakeLists.txt`:
     - Add `libems_rtdb` as a SHARED library from rtdb_lifecycle.c.
     - Link with `-lrt` (for shm_open/shm_unlink) and `-lpthread`.
     - data_manager_c links against libems_rtdb + ems_common_c.
     - Install libems_rtdb.so to lib directory.
     - Set include directory for rtdb_lifecycle.h.

  5. Build and verify: `cmake --build build/ --target data_manager_c libems_rtdb`
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build --target data_manager_c 2>&1 | tail -5</automated>
  </verify>
  <done>libems_rtdb.so builds with create/attach/detach/destroy API, data_manager_c builds and can create RTDB shm with correct header and seqlock initialization</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Python RTDB attach helpers and C/Python integration tests</name>
  <files>
    src/common/python/src/ems_common/rtdb.py,
    tests/test_data_manager.py
  </files>
  <behavior>
    - Test: attach_rtdb() returns (shm, rtdb) with correct magic and version
    - Test: attach_rtdb() calls resource_tracker.unregister to prevent premature cleanup
    - Test: detach_rtdb() unmaps without unlinking
    - Test: Python can read topology counts written by C (run data_manager_c, then attach from Python)
    - Test: Topology validation rejects mismatched counts
    - Test: Round-trip: C writes value to RTDB section, Python reads it via seqlock
  </behavior>
  <action>
  1. Add helpers to `src/common/python/src/ems_common/rtdb.py`:
     - `attach_rtdb() -> tuple[SharedMemory, EmsRtdb]`: Open SharedMemory(name="ems_rtdb", create=False), call `resource_tracker.unregister(f"/{shm.name}", "shared_memory")`, create EmsRtdb.from_buffer(shm.buf), assert magic == RTDB_MAGIC, assert version == RTDB_VERSION. Return (shm, rtdb). Per locked decision and STATE.md: resource_tracker.unregister() is required for Python 3.12.
     - `detach_rtdb(shm: SharedMemory) -> None`: Close shm (shm.close()), do NOT call shm.unlink().
     - `validate_topology(rtdb: EmsRtdb, expected: dict) -> None`: Check cluster_count, racks_per_cluster etc. against expected dict. Raise ValueError on mismatch.

  2. Create `tests/test_data_manager.py`:
     - Use subprocess to run data_manager_c in background for integration tests.
     - Alternative for unit tests: use ctypes to call libems_rtdb.so directly from Python (load with ctypes.CDLL, call rtdb_create, verify via Python attach).
     - Tests:
       a. `test_shm_create`: Call rtdb_create via ctypes, verify /dev/shm/ems_rtdb exists with correct size.
       b. `test_rtdb_init_header`: Create via C, attach from Python, check magic/version/topology.
       c. `test_c_python_attach`: C creates, Python attaches and reads, Python detaches without unlink, C still has valid mapping.
       d. `test_topology_from_config`: Create with specific topology values, verify Python reads same values.
       e. `test_stale_detection`: Create shm with wrong magic manually, then call rtdb_create -- should destroy stale and recreate.
       f. `test_detach_no_unlink`: After detach, shm still exists in /dev/shm.
     - Cleanup: Always rtdb_destroy in fixture teardown to prevent test pollution.
     - Note: Tests need the built libems_rtdb.so -- use `build/src/data_manager/c/libems_rtdb.so` path.

  3. Run: `uv run pytest tests/test_data_manager.py -x -v`
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv run pytest tests/test_data_manager.py -x -v</automated>
  </verify>
  <done>Python attach/detach helpers work with C-created shm, resource_tracker unregister prevents premature cleanup, topology validation works, all integration tests pass</done>
</task>

</tasks>

<verification>
1. C build succeeds: `cmake --build build --target data_manager_c`
2. libems_rtdb.so exports rtdb_create/attach/detach/destroy symbols
3. Python tests pass: `uv run pytest tests/test_data_manager.py -x -v`
4. No test regressions: `uv run pytest tests/test_rtdb.py -x` (existing M0 tests)
5. Full suite: `uv run pytest tests/ -x`
</verification>

<success_criteria>
- data_manager_c creates RTDB shm with correct magic, version, topology from CLI args
- RTDB is zero-filled with all seqlocks initialized to 0
- Stale shm from previous crash is detected (magic/version check) and recreated
- libems_rtdb.so provides create/attach/detach/destroy API
- Python attach_rtdb() with resource_tracker fix reads C-created shm correctly
- Topology counts in header match input parameters
- data_manager_c holds shm open until SIGTERM, then unlinks on shutdown
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation/09-02-SUMMARY.md`
</output>
