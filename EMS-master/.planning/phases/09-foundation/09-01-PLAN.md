---
phase: 09-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config_manager/pyproject.toml
  - src/config_manager/src/ems_config_manager/__init__.py
  - src/config_manager/src/ems_config_manager/manager.py
  - src/config_manager/src/ems_config_manager/overlay.py
  - src/config_manager/src/ems_config_manager/cli.py
  - config/schemas/system_config.schema.json
  - config/system_config.yaml
  - config/profiles/residential/system_config.yaml
  - config/profiles/commercial/system_config.yaml
  - config/profiles/container/system_config.yaml
  - tests/test_config_manager.py
autonomous: true
requirements: [CONF-01, CONF-03, CONF-06, CONF-07]

must_haves:
  truths:
    - "All 14 YAML configs load and validate against JSON Schema at startup; invalid config causes fail-fast with clear error"
    - "Schema version mismatch is detected and rejected with migration guidance"
    - "Deployment profile overlay replaces default config when profile is specified"
    - "CLI command ems-config validate <file> returns pass/fail with detailed errors"
  artifacts:
    - path: "src/config_manager/src/ems_config_manager/manager.py"
      provides: "ConfigManager class with load_all, validate, get_config, get_value"
      min_lines: 100
    - path: "src/config_manager/src/ems_config_manager/overlay.py"
      provides: "Profile overlay loading (full replacement strategy)"
      min_lines: 20
    - path: "src/config_manager/src/ems_config_manager/cli.py"
      provides: "ems-config validate CLI entry point"
      min_lines: 30
    - path: "tests/test_config_manager.py"
      provides: "Unit tests for CONF-01, CONF-03, CONF-06, CONF-07"
      min_lines: 80
  key_links:
    - from: "src/config_manager/src/ems_config_manager/manager.py"
      to: "config/schemas/*.schema.json"
      via: "jsonschema Draft202012Validator"
      pattern: "Draft202012Validator"
    - from: "src/config_manager/src/ems_config_manager/manager.py"
      to: "config/*.yaml"
      via: "yaml.safe_load"
      pattern: "yaml\\.safe_load"
    - from: "src/config_manager/src/ems_config_manager/overlay.py"
      to: "config/profiles/"
      via: "full file replacement"
      pattern: "profiles.*yaml"
---

<objective>
Build config_manager core: startup validation of all 14 YAML configs, schema version checking, deployment profile overlay, and CLI validation tool.

Purpose: Every EMS module depends on validated configuration at startup. This plan delivers the foundation that all later config features (hot-reload, ZMQ API) build on.
Output: Working ConfigManager class, profile overlay, CLI tool, and test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-foundation/09-CONTEXT.md
@.planning/phases/09-foundation/09-RESEARCH.md

@tools/validate_config.py
@src/common/python/src/ems_common/ipc.py
@config/schemas/system_config.schema.json
@src/config_manager/pyproject.toml

<interfaces>
<!-- Existing validation helper to reuse -->
From tools/validate_config.py:
```python
CONFIG_FILES: list[str] = [
    "system_config", "bms_config", "pcs_config", "btms_config",
    "meter_config", "dg_config", "pv_config", "control_config",
    "alarms_config", "schedule_config", "cloud_config", "network_config",
    "gpio_config", "hmi_config",
]

def validate_file(name: str, config_dir: Path) -> list[str]:
    """Returns list of error strings. Empty = valid."""

def format_error(filename: str, error: ValidationError) -> str:
    """Format ValidationError into friendly prose."""
```

From src/common/python/src/ems_common/ipc.py:
```python
TOPIC_CONFIG_RELOAD: str = "config_reload"
def encode_event(...) -> bytes:
def encode_command_request(action: str, params: dict) -> bytes:
def encode_command_response(status: str, result=None, error_msg=None) -> bytes:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema version fields and subsystem presence flags</name>
  <files>
    config/schemas/system_config.schema.json,
    config/system_config.yaml,
    config/profiles/residential/system_config.yaml,
    config/profiles/commercial/system_config.yaml,
    config/profiles/container/system_config.yaml
  </files>
  <behavior>
    - Test: system_config.yaml with correct _schema_version validates OK
    - Test: system_config.yaml with wrong _schema_version fails validation
    - Test: subsystems.has_dg defaults correctly per profile (residential=false, container=true)
  </behavior>
  <action>
  1. Add `_schema_version` field to system_config.schema.json as a required string field with `const: "1.0"`. This field enables CONF-03 schema version mismatch detection.

  2. Add a `subsystems` object to system_config.schema.json with optional boolean fields: `has_dg` (default false), `has_pv` (default false), `has_btms` (default true), `has_meter` (default true). Per CONTEXT.md: config_manager only loads device configs for declared subsystems.

  3. Update all system_config.yaml files (active + 3 profiles):
     - Add `_schema_version: "1.0"` at root
     - Add `subsystems:` section with appropriate defaults:
       - residential: has_dg=false, has_pv=false, has_btms=true, has_meter=true
       - commercial: has_dg=true, has_pv=true, has_btms=true, has_meter=true
       - container: has_dg=true, has_pv=true, has_btms=true, has_meter=true

  4. Add `_schema_version` as a required const field to ALL other 13 schema.json files (bms_config, pcs_config, etc.) with `const: "1.0"`. Update all corresponding YAML files across active config and all 3 profiles to include `_schema_version: "1.0"`.

  5. Run existing validation to confirm no regressions: `uv run python tools/validate_config.py`
  </action>
  <verify>
    <automated>uv run python tools/validate_config.py && uv run python tools/validate_config.py --config-dir config/profiles/residential && uv run python tools/validate_config.py --config-dir config/profiles/commercial && uv run python tools/validate_config.py --config-dir config/profiles/container</automated>
  </verify>
  <done>All 14 schemas have _schema_version const field, system_config has subsystems object, all YAML files updated, existing validation still passes for all profiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfigManager core class with profile overlay and CLI</name>
  <files>
    src/config_manager/pyproject.toml,
    src/config_manager/src/ems_config_manager/__init__.py,
    src/config_manager/src/ems_config_manager/manager.py,
    src/config_manager/src/ems_config_manager/overlay.py,
    src/config_manager/src/ems_config_manager/cli.py,
    tests/test_config_manager.py
  </files>
  <behavior>
    - Test: ConfigManager.load_all() with valid configs succeeds, populates cache with all 14 configs
    - Test: ConfigManager.load_all() with invalid config raises SystemExit or ConfigError (fail-fast)
    - Test: ConfigManager.load_all() with missing optional device config (dg_config) when has_dg=false succeeds
    - Test: ConfigManager.load_all() with missing required core config raises error
    - Test: ConfigManager.get_config("pcs_config") returns full parsed dict
    - Test: ConfigManager.get_value("pcs_config", "connection.protocol") returns "rtu"
    - Test: ConfigManager.get_value("pcs_config", "nonexistent.path") raises descriptive error
    - Test: Schema version mismatch (_schema_version: "2.0") produces clear error with migration guidance
    - Test: Profile overlay loads profile-specific config when profile specified
    - Test: Profile overlay falls back to default config when no profile file exists
    - Test: CLI validate returns exit 0 for valid file, exit 1 with errors for invalid file
  </behavior>
  <action>
  1. Update `src/config_manager/pyproject.toml` to add dependencies: `jsonschema>=4.20`, `pyyaml>=6.0`. Add `[project.scripts]` entry: `ems-config = "ems_config_manager.cli:main"`.

  2. Create `src/config_manager/src/ems_config_manager/overlay.py`:
     - Function `load_with_profile(name: str, config_dir: Path, profile: str | None) -> dict` that uses full file replacement strategy (per research recommendation and Claude's discretion). If profile is specified and profile file exists at `config_dir/profiles/{profile}/{name}.yaml`, load that instead. Otherwise load `config_dir/{name}.yaml`.

  3. Create `src/config_manager/src/ems_config_manager/manager.py`:
     - Class `ConfigManager` with constructor taking `config_dir: Path`, `schema_dir: Path`, `profile: str | None`.
     - Core configs list (10 mandatory): system_config, gpio_config, control_config, alarms_config, schedule_config, bms_config, pcs_config, network_config, cloud_config, hmi_config.
     - Optional device configs (4): dg_config, pv_config, btms_config, meter_config -- loaded only if system_config.subsystems.has_{device} is true (per locked decision).
     - `load_all() -> None`: Load system_config first (to read subsystems flags), then load remaining. For each file: (a) check _schema_version matches schema's const value -- if mismatch, raise with migration guidance message, (b) validate against JSON Schema with Draft202012Validator, (c) on any error, fail-fast with clear message (per locked decision). Store validated configs in `_configs: dict[str, dict]`.
     - `get_config(name: str) -> dict`: Return from cache. Raise KeyError with message if not loaded.
     - `get_value(name: str, path: str) -> Any`: Navigate dotted path (e.g., "connection.protocol"). Return value. On missing path, raise descriptive error with available keys (per locked decision).
     - Reuse `format_error()` from tools/validate_config.py or implement equivalent.

  4. Create `src/config_manager/src/ems_config_manager/cli.py`:
     - `main()` function as CLI entry point using argparse.
     - Subcommand `validate` taking a file path argument (single file or directory).
     - For single file: validate against corresponding schema, print OK or detailed errors.
     - For directory: validate all 14 files, print summary.
     - Return exit code 0 on success, 1 on failure.

  5. Update `__init__.py` to export ConfigManager.

  6. Create `tests/test_config_manager.py` with tests for all behaviors listed above. Use tmp_path fixture to create test config files. Test both happy path and error paths.

  Run `uv sync --all-packages` after updating pyproject.toml.
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv sync --all-packages && uv run pytest tests/test_config_manager.py -x -v</automated>
  </verify>
  <done>ConfigManager loads and validates all configs at startup with fail-fast, supports profile overlay, provides get_config/get_value from cache, CLI validates individual files, schema version mismatch detected -- all tests pass</done>
</task>

</tasks>

<verification>
1. All 14 YAML configs validate with `uv run python tools/validate_config.py` (including new _schema_version fields)
2. ConfigManager test suite passes: `uv run pytest tests/test_config_manager.py -x -v`
3. CLI works: `uv run ems-config validate config/system_config.yaml` returns OK
4. Full test suite has no regressions: `uv run pytest tests/ -x`
</verification>

<success_criteria>
- ConfigManager.load_all() validates all 14 configs against schemas with fail-fast behavior
- Schema version mismatch produces clear error with migration guidance
- Profile overlay loads correct config when profile specified via full file replacement
- get_config() and get_value() serve from in-memory cache with descriptive errors on missing
- ems-config validate CLI returns pass/fail with detailed errors
- Optional device configs skipped when subsystem not declared in system_config
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation/09-01-SUMMARY.md`
</output>
