---
phase: 28-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hmi_server/src/ems_hmi_server/ws.py
  - src/hmi_server/src/ems_hmi_server/app.py
  - src/hmi_server/src/ems_hmi_server/schedule.py
  - src/hmi_server/tests/conftest.py
  - src/hmi_server/tests/test_ws.py
  - src/hmi_server/tests/test_schedule.py
  - config/hmi_config.yaml
  - config/schemas/hmi_config.schema.json
autonomous: true
requirements:
  - PROD-05

must_haves:
  truths:
    - "WebSocket bridge subscribes to SOCK_CLOUD_PUB and SOCK_OTA_PUB alongside SOCK_TELEMETRY"
    - "Cloud and OTA telemetry messages are broadcast to all WebSocket clients with {topic, data, ts} envelope"
    - "PUT /api/config/schedule validates body against JSON Schema and writes YAML (triggers hot-reload)"
    - "PUT /api/config/schedule returns 403 for non-admin and 422 for invalid body"
    - "websocket_port field removed from schema and config (schema v2.0)"
  artifacts:
    - path: "src/hmi_server/src/ems_hmi_server/ws.py"
      provides: "Multi-source ZMQ poller bridge"
      contains: "zmq.asyncio.Poller"
    - path: "src/hmi_server/src/ems_hmi_server/schedule.py"
      provides: "PUT /api/config/schedule endpoint"
      exports: ["router"]
    - path: "src/hmi_server/tests/test_schedule.py"
      provides: "Schedule endpoint tests"
      contains: "test_update_schedule"
    - path: "config/schemas/hmi_config.schema.json"
      provides: "Schema v2.0 without websocket_port"
      contains: "\"2.0\""
  key_links:
    - from: "src/hmi_server/src/ems_hmi_server/ws.py"
      to: "ems_common.ipc"
      via: "import SOCK_CLOUD_PUB, SOCK_OTA_PUB, TOPIC_CLOUD, TOPIC_OTA"
      pattern: "SOCK_CLOUD_PUB|SOCK_OTA_PUB"
    - from: "src/hmi_server/src/ems_hmi_server/app.py"
      to: "ws.telemetry_bridge"
      via: "passes cloud_socket and ota_socket to bridge"
      pattern: "cloud_socket|ota_socket"
    - from: "src/hmi_server/src/ems_hmi_server/schedule.py"
      to: "config/schemas/schedule_config.schema.json"
      via: "jsonschema.validate against schema file"
      pattern: "schedule_config\\.schema\\.json"
---

<objective>
Backend changes for HMI cloud/OTA status integration, schedule save endpoint, and schema tech debt cleanup.

Purpose: Wire SOCK_CLOUD_PUB and SOCK_OTA_PUB into the WebSocket bridge so frontend can display cloud/OTA status. Add schedule save endpoint to replace placeholder. Remove dead websocket_port config field.
Output: Updated ws.py with multi-source poller, new schedule.py router, cleaned schema v2.0, passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-production-hardening/28-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/common/python/src/ems_common/ipc.py:
```python
SOCK_TELEMETRY: str = "ipc:///run/ems/telemetry.sock"
SOCK_CLOUD_PUB: str = "ipc:///run/ems/cloud_pub.sock"
SOCK_OTA_PUB: str = "ipc:///run/ems/ota_pub.sock"
TOPIC_CLOUD: str = "cloud"
TOPIC_OTA: str = "ota"
# Existing topics: TOPIC_BMS_RACK, TOPIC_PCS, TOPIC_GPIO, TOPIC_METER, TOPIC_BTMS, TOPIC_SYSTEM
def encode_telemetry(ts: int, seq: int, source: str, topic: str, payload: dict) -> bytes: ...
def decode_telemetry(data: bytes) -> dict: ...
```

From src/hmi_server/src/ems_hmi_server/ws.py (current signature to modify):
```python
async def telemetry_bridge(
    zmq_ctx: zmq.asyncio.Context,
    client_manager: ClientManager,
    socket_path: str,
) -> None:
```

From src/hmi_server/src/ems_hmi_server/app.py (current lifespan bridge creation):
```python
telemetry_socket: str = getattr(app.state, "telemetry_socket", SOCK_TELEMETRY)
bridge_task: asyncio.Task = asyncio.create_task(
    telemetry_bridge(app.state.zmq_ctx, app.state.client_manager, telemetry_socket)
)
```

From src/hmi_server/src/ems_hmi_server/deps.py:
```python
async def require_admin(auth: dict = Depends(require_auth)) -> dict:
    if auth["level"] != "admin":
        raise HTTPException(status_code=403, detail="Admin access required")
    return auth
```

From src/hmi_server/tests/conftest.py (test_config to update):
```python
def test_config() -> dict:
    return {
        "server": {"http_port": 8080, "websocket_port": 8081, "host": "0.0.0.0"},
        ...
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multi-source ZMQ poller bridge + schema cleanup</name>
  <files>
    src/hmi_server/src/ems_hmi_server/ws.py,
    src/hmi_server/src/ems_hmi_server/app.py,
    src/hmi_server/tests/conftest.py,
    src/hmi_server/tests/test_ws.py,
    config/hmi_config.yaml,
    config/schemas/hmi_config.schema.json
  </files>
  <behavior>
    - test_bridge_receives_cloud_topic: PUB cloud topic on separate socket -> bridge broadcasts {topic: "cloud", data: {state: "connected", broker: "test", ts: ...}, ts: ...} to client queue
    - test_bridge_receives_ota_topic: PUB ota topic on separate socket -> bridge broadcasts {topic: "ota", data: {state: "idle", ...}, ts: ...} to client queue
    - test_bridge_receives_all_sources: Send telemetry on telemetry socket + cloud on cloud socket + ota on ota socket -> all 3 received by client
    - test_bridge_cancellation_closes_all_sockets: Cancel bridge -> all 3 SUB sockets closed cleanly, context terminates
    - Existing tests continue to pass (bridge still handles all 6 telemetry topics)
  </behavior>
  <action>
    1. **ws.py** — Rewrite `telemetry_bridge` to use `zmq.asyncio.Poller`:
       - Change signature to: `async def telemetry_bridge(zmq_ctx, client_manager, socket_path, cloud_socket_path="", ota_socket_path="") -> None`
       - Import `TOPIC_CLOUD, TOPIC_OTA` from `ems_common.ipc`
       - Create 3 SUB sockets: `sub_telemetry` (subscribes to all `_TELEMETRY_TOPICS`), `sub_cloud` (subscribes to `TOPIC_CLOUD`), `sub_ota` (subscribes to `TOPIC_OTA`)
       - Only create cloud/ota sockets if their paths are non-empty (backward compat for tests that pass only telemetry socket)
       - Register all active sockets on `zmq.asyncio.Poller`
       - In poll loop: `socks = dict(await poller.poll())`, iterate registered sockets, recv_multipart + decode + broadcast
       - Close all sockets in finally block

    2. **app.py** — Update lifespan and create_app:
       - Import `SOCK_CLOUD_PUB, SOCK_OTA_PUB` from `ems_common.ipc`
       - In lifespan, resolve `cloud_socket` and `ota_socket` via `getattr(app.state, "cloud_socket", SOCK_CLOUD_PUB)` (same pattern as telemetry_socket)
       - Pass `cloud_socket_path=cloud_socket, ota_socket_path=ota_socket` to `telemetry_bridge`
       - Update `create_app` signature: add `cloud_socket: str | None = None, ota_socket: str | None = None` params
       - Store on `app.state` if provided (for test override)
       - Include `schedule_router` from `ems_hmi_server.schedule`

    3. **conftest.py** — Remove `websocket_port` from `test_config["server"]`. Update `create_app` calls to pass cloud/ota socket overrides where needed. Add `mock_zmq_cloud_pub` and `mock_zmq_ota_pub` fixtures similar to existing `mock_zmq_pub`.

    4. **test_ws.py** — Add 4 new tests per behavior block above. Tests create 3 separate PUB sockets (telemetry, cloud, ota) and verify the bridge receives from all sources. Use the pattern from existing `test_bridge_receives_and_broadcasts` but with 3 sockets.

    5. **config/schemas/hmi_config.schema.json** — Remove `websocket_port` from `server.properties` and from `server.required`. Bump `_schema_version` const from `"1.0"` to `"2.0"`.

    6. **config/hmi_config.yaml** — Remove `websocket_port: 8081` line. Update `_schema_version` from `"1.0"` to `"2.0"`.

    IMPORTANT: The cloud/ota socket paths should default to empty string `""` in the bridge signature (not the IPC paths) so tests that only provide a telemetry socket still work without connecting to non-existent IPC endpoints. The app.py lifespan passes the real defaults.
  </action>
  <verify>
    <automated>cd /home/overlord/EMS/src/hmi_server && uv run pytest tests/test_ws.py -x -q</automated>
  </verify>
  <done>
    - telemetry_bridge uses zmq.asyncio.Poller across 3 SUB sockets
    - Cloud and OTA topics delivered to WebSocket clients
    - websocket_port removed from schema (v2.0) and config YAML
    - conftest test_config matches new schema (no websocket_port)
    - All existing + 4 new ws tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schedule save endpoint</name>
  <files>
    src/hmi_server/src/ems_hmi_server/schedule.py,
    src/hmi_server/tests/test_schedule.py
  </files>
  <behavior>
    - test_update_schedule_valid: PUT /api/config/schedule with valid body (admin auth) -> 200, {"status": "ok", "result": {"written": true}}
    - test_update_schedule_invalid_body: PUT /api/config/schedule with body failing schema validation -> 422
    - test_update_schedule_forbidden: PUT /api/config/schedule with operator-level auth -> 403
    - test_update_schedule_writes_yaml: After successful PUT, config/schedule_config.yaml contains the submitted data
  </behavior>
  <action>
    1. **schedule.py** (NEW) — Create FastAPI router with PUT `/api/config/schedule`:
       - Import `json, jsonschema, yaml` from stdlib/deps and `Path` from pathlib
       - Import `require_admin` from `ems_hmi_server.deps`
       - Import `Body, Depends, HTTPException, Request` from fastapi
       - Router: `router = APIRouter()`
       - Endpoint loads `config/schemas/schedule_config.schema.json`, validates body with `jsonschema.validate(body, schema)`, catches `jsonschema.ValidationError` -> raise `HTTPException(422, detail=str(exc.message))`
       - On success: `Path("config/schedule_config.yaml").write_text(yaml.dump(body, default_flow_style=False))`
       - Return `{"status": "ok", "result": {"written": True}, "error_msg": None}`
       - Use `Depends(require_admin)` for auth
       - Schema path should be configurable via `request.app.state.config` or env, but for simplicity resolve relative to WorkingDirectory (consistent with existing pattern)

    2. **test_schedule.py** (NEW) — 4 tests per behavior block:
       - Use `async_client` fixture from conftest
       - For valid test: authenticate as admin first (POST /api/auth/login with admin PIN), use token
       - For forbidden test: authenticate as operator
       - For invalid body test: send body that violates schedule schema (e.g., missing required fields)
       - For YAML write test: use `tmp_path` fixture, monkeypatch the config path
       - Use existing auth pattern from test_control.py or test_alarm.py for getting auth tokens

    3. **app.py** — Add `from ems_hmi_server.schedule import router as schedule_router` and `app.include_router(schedule_router)` in `create_app`. (Already noted in Task 1 action — do it in whichever task executes second, or in this task if Task 1 hasn't included it.)
  </action>
  <verify>
    <automated>cd /home/overlord/EMS/src/hmi_server && uv run pytest tests/test_schedule.py -x -q</automated>
  </verify>
  <done>
    - PUT /api/config/schedule validates against JSON Schema, writes YAML on success
    - Returns 403 for non-admin, 422 for invalid body, 200 with {status: "ok"} for valid
    - Config_manager hot-reload triggered by file write (no explicit notification needed)
    - All 4 schedule tests pass
  </done>
</task>

</tasks>

<verification>
Run full backend test suite to confirm no regressions:
```bash
cd /home/overlord/EMS/src/hmi_server && uv run pytest tests/ -v
```
All tests pass including existing auth, control, alarm, query, health, app, and ws tests.
</verification>

<success_criteria>
- WebSocket bridge polls 3 ZMQ sources (telemetry + cloud + ota) via zmq.asyncio.Poller
- Cloud and OTA messages delivered to all WebSocket clients in standard {topic, data, ts} format
- PUT /api/config/schedule works with admin auth, validates, writes YAML
- websocket_port removed from config and schema (v2.0)
- All backend tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/28-production-hardening/28-01-SUMMARY.md`
</output>
