---
phase: 19-hmi-frontend
plan: 05
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - src/hmi_server/frontend/src/components/charts/EnergyBar.tsx
  - src/hmi_server/frontend/src/screens/EnergyScreen.tsx
  - src/hmi_server/frontend/src/screens/SettingsScreen.tsx
  - src/hmi_server/frontend/src/__tests__/EnergyScreen.test.tsx
  - src/hmi_server/frontend/src/__tests__/SettingsScreen.test.tsx
autonomous: true
requirements: [HMI-10, HMI-11]

must_haves:
  truths:
    - "Energy screen shows charge/discharge totals fetched from REST query"
    - "Energy screen renders bar chart for daily/weekly/monthly totals"
    - "Energy screen allows selecting time range (today, 7 days, 30 days)"
    - "Settings screen is only accessible to admin users"
    - "Settings screen shows schedule form with time windows"
    - "Settings screen schedule form matches ScheduleConfig interface"
    - "Full build succeeds with zero TypeScript errors"
    - "All tests pass across all 5 plans"
  artifacts:
    - path: "src/hmi_server/frontend/src/components/charts/EnergyBar.tsx"
      provides: "Bar chart for energy totals (charge/discharge/import/export)"
      exports: ["EnergyBar"]
    - path: "src/hmi_server/frontend/src/screens/EnergyScreen.tsx"
      provides: "Energy totals display with time range selector and bar chart"
    - path: "src/hmi_server/frontend/src/screens/SettingsScreen.tsx"
      provides: "Admin-only settings with schedule editor form"
  key_links:
    - from: "src/hmi_server/frontend/src/screens/EnergyScreen.tsx"
      to: "/api/query/energy_totals"
      via: "POST with start_ts/end_ts on mount and range change"
      pattern: "apiFetch.*/api/query/energy_totals"
    - from: "src/hmi_server/frontend/src/screens/SettingsScreen.tsx"
      to: "src/hmi_server/frontend/src/types/api.ts"
      via: "Uses ScheduleConfig and TimeWindow interfaces for form shape"
      pattern: "ScheduleConfig"
---

<objective>
Energy + Settings screens and full build verification: Energy totals with bar chart, admin-only settings with schedule form, complete test suite green.

Purpose: Complete the final two screens and verify the entire 7-screen frontend builds and tests cleanly. Energy screen gives operators visibility into charge/discharge history. Settings screen (admin-only) allows schedule configuration.

Output: All 7 screens functional, full test suite passing, production build succeeding.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-hmi-frontend/19-CONTEXT.md
@.planning/phases/19-hmi-frontend/19-RESEARCH.md
@.planning/phases/19-hmi-frontend/19-04-SUMMARY.md

<interfaces>
From src/hmi_server/frontend/src/types/api.ts:
```typescript
export interface ScheduleConfig {
  mode: "manual" | "time_of_day" | "curve";
  time_windows: TimeWindow[];
  day_night: { day_start: string; night_start: string; };
  power_curve: number[];
}
export interface TimeWindow {
  start: string; end: string;
  action: "charge" | "discharge" | "idle";
  power_kw: number;
}
export interface ZmqResponse {
  status: "ok" | "error";
  result: Record<string, unknown> | null;
  error_msg: string | null;
}
```

From src/hmi_server/frontend/src/hooks/useApi.ts:
```typescript
export function useApi(): { apiFetch: <T>(path: string, options?: RequestInit) => Promise<T> };
```

From src/hmi_server/frontend/src/hooks/useAuth.ts (via AuthContext):
```typescript
export function useAuth(): { state: { token: string | null; level: "operator" | "admin" | null; expiresAt: number | null }, login: ..., logout: ... };
```

REST API contracts:
- POST /api/query/energy_totals body: { start_ts: number, end_ts: number }
  Response result: { columns: ["discharge_wh","charge_wh","grid_import_wh","grid_export_wh"], rows: [[...]], count: 1 }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Energy screen with bar chart and time range selector</name>
  <files>
    src/hmi_server/frontend/src/components/charts/EnergyBar.tsx
    src/hmi_server/frontend/src/screens/EnergyScreen.tsx
    src/hmi_server/frontend/src/__tests__/EnergyScreen.test.tsx
  </files>
  <behavior>
    - EnergyScreen fetches energy totals from POST /api/query/energy_totals on mount
    - EnergyScreen renders time range selector buttons: Today, 7 Days, 30 Days
    - Changing time range re-fetches data with new start_ts/end_ts
    - Energy totals displayed as cards: Charge (kWh), Discharge (kWh), Grid Import (kWh), Grid Export (kWh)
    - EnergyBar renders a grouped bar chart from the energy data
    - Values converted from Wh to kWh (divide by 1000) for display
    - Loading state shown while fetching
    - Error state shown if query fails
  </behavior>
  <action>
    1. Create `components/charts/EnergyBar.tsx`:
       - Props: `{ charge_kwh: number, discharge_kwh: number, import_kwh: number, export_kwh: number, width?: number, height?: number }`
       - Uses `<Bar>` from react-chartjs-2
       - 4 bars: Charge (green), Discharge (orange), Import (blue), Export (purple)
       - Options: animation: false, responsive: false, indexAxis: "x" (vertical bars)
       - Fixed canvas dimensions: width 400, height 250 (configurable via props)
       - plugins: { legend: { display: true, position: "bottom", labels: { color: "#94a3b8" } } }

    2. Replace placeholder `screens/EnergyScreen.tsx`:
       - Call `useApi()` for REST fetch
       - **Time range selector** (top): 3 buttons -- Today, 7 Days, 30 Days
         - Each button >= 44x44px, active:bg-accent, selected gets bg-accent
         - Default: Today
         - Calculate start_ts: now minus selected range (in milliseconds)
         - On change: POST `/api/query/energy_totals` with { start_ts, end_ts: Date.now() }

       - **Energy totals cards** (middle, 2x2 grid):
         - Charge: green accent, value in kWh (result.rows[0][1] / 1000)
         - Discharge: orange accent, value in kWh (result.rows[0][0] / 1000)
         - Grid Import: blue accent, value in kWh (result.rows[0][2] / 1000)
         - Grid Export: purple accent, value in kWh (result.rows[0][3] / 1000)
         - Use Intl.NumberFormat for display (1 decimal place, append " kWh")

       - **Bar chart** (bottom): EnergyBar component with the 4 values

       - **States**:
         - Loading: "Fetching energy data..." spinner/text
         - Error: "Failed to load energy data" with retry button
         - No data: "No energy data for this period"
         - Parse response: extract from result.columns and result.rows to get named values

    3. Write `__tests__/EnergyScreen.test.tsx`:
       - Mock useApi to return test energy data
       - Test: renders time range buttons (Today, 7 Days, 30 Days)
       - Test: fetches energy_totals on mount with today's range
       - Test: clicking 7 Days button re-fetches with correct start_ts
       - Test: renders energy total values in kWh
       - Test: handles loading state
       - Test: handles error/empty state
       - Mock EnergyBar chart component to avoid canvas issues
  </action>
  <verify>
    <automated>cd src/hmi_server/frontend && bun run test -- --run src/__tests__/EnergyScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Energy screen fetches and displays charge/discharge/import/export totals
    - Time range selector works (Today/7D/30D)
    - Bar chart renders energy data
    - Loading/error/empty states handled
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings screen (admin-only) with schedule form, full build verification</name>
  <files>
    src/hmi_server/frontend/src/screens/SettingsScreen.tsx
    src/hmi_server/frontend/src/__tests__/SettingsScreen.test.tsx
  </files>
  <behavior>
    - SettingsScreen redirects non-admin users to Dashboard
    - SettingsScreen renders schedule mode selector (manual, time_of_day, curve)
    - Time windows form: add/remove windows with start time, end time, action, power_kw
    - Day/night settings: day_start and night_start time inputs
    - Save button shows "Save not yet implemented" message (no backend endpoint yet per RESEARCH.md open question)
    - Form values match ScheduleConfig interface shape
    - Full test suite passes across all plans
    - Production build succeeds with zero errors
  </behavior>
  <action>
    1. Replace placeholder `screens/SettingsScreen.tsx`:
       - Call `useAuth()` to check admin level
       - If level !== "admin": redirect to "/" using `useNavigate()` from react-router-dom (or show "Admin access required" message)

       - **Schedule Mode selector** (top section):
         - 3 buttons: Manual, Time of Day, Curve
         - Selected mode highlighted with bg-accent
         - Each >= 44x44px

       - **Time Windows editor** (visible when mode === "time_of_day"):
         - List of current time windows, each showing: start, end, action, power_kw
         - "Add Window" button at bottom
         - Each window row has:
           - Start time: text display with edit button that opens a time picker (simple HH:MM input or NumericKeypad for hours/minutes)
           - End time: same
           - Action: 3 toggle buttons (charge/discharge/idle)
           - Power (kW): text display with edit button opening NumericKeypad
           - Remove button (X icon, danger color)
         - All interactive elements >= 44x44px

       - **Day/Night settings** (visible when mode === "time_of_day"):
         - Day start time (default "06:00")
         - Night start time (default "18:00")
         - Display as editable text with NumericKeypad for HH:MM entry

       - **Power Curve** (visible when mode === "curve"):
         - Read-only display: "96-point power curve editing is deferred to a future release"
         - Show current curve values if loaded (list or simple text)

       - **System Info section** (always visible):
         - Display build version from package.json (import)
         - Show EMS uptime from system telemetry if available

       - **Save button**:
         - Renders at bottom, bg-accent, >= 44x44px
         - On press: show inline message "Schedule save endpoint not yet available. Configuration will be saved when the backend API is implemented." (per RESEARCH.md open question #1)
         - Style as informational (yellow/warning), not error

       - **Logout button**: bg-danger, calls useAuth().logout()

    2. Write `__tests__/SettingsScreen.test.tsx`:
       - Mock useAuth with admin level
       - Mock useApi for potential fetch calls
       - Test: renders schedule mode selector buttons
       - Test: selecting "time_of_day" mode shows time windows section
       - Test: add window button adds a new window row
       - Test: remove button removes a window row
       - Test: non-admin user sees redirect or "Admin access required"
       - Test: save button shows "not yet available" message
       - Test: logout button calls logout function

    3. Run full verification:
       ```bash
       cd src/hmi_server/frontend && bun run build && bun run test -- --run
       ```
       Ensure ALL tests from all 5 plans pass and build produces dist/ with zero errors.
  </action>
  <verify>
    <automated>cd src/hmi_server/frontend && bun run build && bun run test -- --run 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Settings screen admin-gated (non-admin redirected)
    - Schedule mode selector works (manual/time_of_day/curve)
    - Time window form: add/remove/edit windows
    - Save shows "not yet available" message
    - Logout button functional
    - ALL tests across all 5 plans pass
    - Production build succeeds with zero TypeScript errors
    - dist/ directory contains built React app ready for FastAPI serving
  </done>
</task>

</tasks>

<verification>
```bash
cd src/hmi_server/frontend && bun run build && bun run test -- --run
```
Full build succeeds. Complete test suite passes. dist/ directory produced.

Final check -- verify all 7 screens are in the build:
```bash
ls -la src/hmi_server/frontend/dist/
```
</verification>

<success_criteria>
- Energy screen displays totals with bar chart and time range selector
- Settings screen admin-gated with schedule form editor
- Save action gracefully handles missing backend endpoint
- Full test suite passes (all plans: Layout, WebSocket, Telemetry, Auth, RollingBuffer, Dashboard, BMS, PCS, Alarm, Control, Energy, Settings)
- `bun run build` produces dist/ with zero errors
- All 9 requirements (HMI-05 through HMI-13) addressed across plans 01-05
</success_criteria>

<output>
After completion, create `.planning/phases/19-hmi-frontend/19-05-SUMMARY.md`
</output>
