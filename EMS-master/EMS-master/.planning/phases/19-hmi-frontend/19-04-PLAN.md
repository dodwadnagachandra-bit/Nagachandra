---
phase: 19-hmi-frontend
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - src/hmi_server/frontend/src/screens/AlarmScreen.tsx
  - src/hmi_server/frontend/src/screens/ControlScreen.tsx
  - src/hmi_server/frontend/src/components/Layout.tsx
  - src/hmi_server/frontend/src/__tests__/AlarmScreen.test.tsx
  - src/hmi_server/frontend/src/__tests__/ControlScreen.test.tsx
autonomous: true
requirements: [HMI-08, HMI-09]

must_haves:
  truths:
    - "Alarm screen shows active alarms table with severity-colored rows"
    - "Alarm table is sortable by severity and timestamp"
    - "Alarm ACK button sends POST to /api/alarm/acknowledge"
    - "Alarm history section queries event_log via REST"
    - "Control screen shows current control state from system telemetry"
    - "Control mode selector shows available states and requires confirmation before switching"
    - "Control setpoint input uses NumericKeypad for touch-friendly entry"
    - "Maintenance enter/exit and fault reset buttons send REST commands"
    - "Sidebar alarm badge shows count of ACTIVE_UNACKED alarms"
  artifacts:
    - path: "src/hmi_server/frontend/src/screens/AlarmScreen.tsx"
      provides: "Alarm management with active table, history, and ACK"
    - path: "src/hmi_server/frontend/src/screens/ControlScreen.tsx"
      provides: "Control interface with mode selector, setpoint, maintenance buttons"
  key_links:
    - from: "src/hmi_server/frontend/src/screens/AlarmScreen.tsx"
      to: "/api/alarm/active"
      via: "GET request on mount and periodic refresh"
      pattern: "apiFetch.*/api/alarm/active"
    - from: "src/hmi_server/frontend/src/screens/AlarmScreen.tsx"
      to: "/api/alarm/acknowledge"
      via: "POST on ACK button press"
      pattern: "apiFetch.*/api/alarm/acknowledge"
    - from: "src/hmi_server/frontend/src/screens/ControlScreen.tsx"
      to: "/api/control/mode"
      via: "POST on mode change confirmation"
      pattern: "apiFetch.*/api/control/mode"
    - from: "src/hmi_server/frontend/src/screens/ControlScreen.tsx"
      to: "/api/control/setpoint"
      via: "POST on setpoint confirmation"
      pattern: "apiFetch.*/api/control/setpoint"
---

<objective>
Alarm + Control screens: Active alarm table with sort/ACK, alarm history query, control state display, mode selector with confirmation, setpoint input, maintenance/fault buttons.

Purpose: Implement the two interactive operator screens that allow alarm management (view, acknowledge) and system control (mode changes, setpoints, maintenance). These are the primary action screens.

Output: Working alarm screen with live alarm table and ACK, control screen with mode selector and setpoint entry, sidebar alarm badge wired.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-hmi-frontend/19-CONTEXT.md
@.planning/phases/19-hmi-frontend/19-RESEARCH.md
@.planning/phases/19-hmi-frontend/19-03-SUMMARY.md

<interfaces>
From src/hmi_server/frontend/src/types/api.ts:
```typescript
export interface ActiveAlarm {
  alarm_id: string; signal: string;
  severity: "warning" | "action" | "protection";
  state: "ACTIVE_UNACKED" | "ACTIVE_ACKED" | "CLEARED_UNACKED";
  activated_at: number;
}
export interface AcknowledgeRequest { alarm_id: string; }
export interface ModeChangeRequest { target_state: string; }
export interface ManualSetpointRequest { power_kw: number; }
export interface MaintenanceRequest { action: "enter" | "exit"; }
export interface ZmqResponse { status: "ok" | "error"; result: Record<string, unknown> | null; error_msg: string | null; }
```

From src/hmi_server/frontend/src/types/telemetry.ts:
```typescript
export interface SystemTelemetry {
  control_state: number; active_setpoint_kw: number; /* ... */
}
```

From src/hmi_server/frontend/src/types/enums.ts:
```typescript
export const CONTROL_STATE_NAMES: Record<number, string>;
// 1=IDLE, 2=STANDBY, 3=CHARGING, 4=DISCHARGING, 5=FAULT, 6=EMERGENCY, 7=MAINTENANCE
```

From src/hmi_server/frontend/src/hooks/useApi.ts:
```typescript
export function useApi(): { apiFetch: <T>(path: string, options?: RequestInit) => Promise<T> };
```

From src/hmi_server/frontend/src/hooks/useTelemetry.ts:
```typescript
export function useTelemetry(topic: "system"): SystemTelemetry | null;
```

From src/hmi_server/frontend/src/components/NumericKeypad.tsx:
```typescript
// Props: { value, onChange, onConfirm, onCancel, title?, maxLength? }
```

From src/hmi_server/frontend/src/components/ConfirmDialog.tsx:
```typescript
// Props: { title, message, onConfirm, onCancel, confirmLabel?, cancelLabel?, variant? }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alarm screen -- active alarm table, history query, ACK button, sidebar badge</name>
  <files>
    src/hmi_server/frontend/src/screens/AlarmScreen.tsx
    src/hmi_server/frontend/src/components/Layout.tsx
    src/hmi_server/frontend/src/__tests__/AlarmScreen.test.tsx
  </files>
  <behavior>
    - AlarmScreen fetches active alarms from GET /api/alarm/active on mount
    - AlarmScreen renders a table with columns: severity, signal, state, time, action
    - Alarm rows are color-coded by severity: red bg for protection, orange bg for action, yellow bg for warning
    - Table is sortable by clicking column headers (severity, time)
    - ACTIVE_UNACKED alarms show an ACK button in the action column
    - ACK button sends POST /api/alarm/acknowledge with alarm_id, then refreshes the table
    - Alarm history tab/section sends POST /api/query/event_log with time range and displays results
    - AlarmScreen auto-refreshes active alarms every 5 seconds
    - Sidebar alarm badge shows count of ACTIVE_UNACKED alarms
    - Empty state: "No active alarms" message
  </behavior>
  <action>
    1. Replace placeholder `screens/AlarmScreen.tsx`:
       - Two sections (tabs or toggle): "Active" and "History"
       - **Active alarms section:**
         - On mount: GET `/api/alarm/active` via useApi. Parse result for alarm list.
         - Auto-refresh every 5 seconds using setInterval (alarms are not on WebSocket)
         - Render table with columns: Severity (icon/badge), Signal, State, Activated At, Action
         - Row colors using Tailwind: protection = `bg-red-900/30 border-l-4 border-red-500`, action = `bg-orange-900/30 border-l-4 border-orange-500`, warning = `bg-yellow-900/30 border-l-4 border-yellow-500`
         - Sortable columns: click header to toggle sort (severity priority: protection > action > warning, or by activated_at desc/asc)
         - ACTIVE_UNACKED rows: render ACK button (>= 44x44px, bg-accent). On press: POST `/api/alarm/acknowledge` with { alarm_id }, then re-fetch active alarms.
         - ACTIVE_ACKED rows: show "Acked" badge, no button
         - CLEARED_UNACKED rows: show "Cleared" badge with ACK button
         - Format activated_at timestamp using Intl.DateTimeFormat (HH:MM:SS)
         - Scroll enabled on this table (per CONTEXT.md decision)
         - Show 6 rows on 10", 12 rows on 15" (use max-h with overflow-y-auto)
         - Empty state: "No active alarms" centered text

       - **History section:**
         - Date range: last 24 hours by default
         - On section mount: POST `/api/query/event_log` with { start_ts, end_ts } via useApi
         - Parse columns/rows response and render as generic table
         - Apply same severity coloring if severity column present
         - Optional severity filter buttons (all / warning / action / protection)
         - Scroll enabled

    2. Update `components/Layout.tsx`:
       - Fetch active alarms count for sidebar badge:
         - Use useApi to GET `/api/alarm/active` on an interval (every 10 seconds)
         - Count alarms where state === "ACTIVE_UNACKED"
         - Pass count to Sidebar as unackedCount prop
         - Handle errors silently (don't break layout if alarm fetch fails)

    3. Write `__tests__/AlarmScreen.test.tsx`:
       - Mock useApi to return test alarm data
       - Test: renders table with correct number of rows
       - Test: severity colors applied to rows
       - Test: ACK button visible for ACTIVE_UNACKED alarms
       - Test: clicking ACK calls apiFetch with correct alarm_id
       - Test: empty state message when no alarms
       - Test: sort toggles when clicking column header
  </action>
  <verify>
    <automated>cd src/hmi_server/frontend && bun run test -- --run src/__tests__/AlarmScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Active alarm table renders with severity-colored rows
    - Sortable by severity and timestamp
    - ACK button sends POST and refreshes table
    - History section queries event_log and displays results
    - Sidebar badge shows ACTIVE_UNACKED count
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Control screen -- state display, mode selector, setpoint input, maintenance/fault buttons</name>
  <files>
    src/hmi_server/frontend/src/screens/ControlScreen.tsx
    src/hmi_server/frontend/src/__tests__/ControlScreen.test.tsx
  </files>
  <behavior>
    - ControlScreen shows current control state from system telemetry (IDLE, STANDBY, etc.)
    - Mode selector shows available target states as buttons
    - Clicking a mode button opens ConfirmDialog before sending command
    - After confirmation, sends POST /api/control/mode with target_state
    - Setpoint input opens NumericKeypad for power_kw entry
    - Setpoint confirmation sends POST /api/control/setpoint
    - Maintenance enter/exit button sends POST /api/control/maintenance
    - Fault reset button sends POST /api/control/fault-reset
    - Success/error feedback shown as toast or inline message
    - All buttons >= 44x44px, no hover states
  </behavior>
  <action>
    1. Replace placeholder `screens/ControlScreen.tsx`:
       - Call `useTelemetry("system")` for current control_state and active_setpoint_kw
       - Call `useAuth()` to check admin level for maintenance button visibility

       - **Current State display** (top section):
         - Large text showing CONTROL_STATE_NAMES[control_state] with color coding
         - Active setpoint: "{active_setpoint_kw} kW" display
         - Color: green for IDLE/STANDBY/CHARGING/DISCHARGING, red for FAULT/EMERGENCY, yellow for MAINTENANCE

       - **Mode Selector** (middle section):
         - 4 buttons in a row: IDLE, STANDBY, CHARGING, DISCHARGING
         - Current state button disabled/highlighted
         - Each button >= 44x44px, bg-bg-card, active:bg-accent
         - On press: open ConfirmDialog with message "Change mode to {target_state}?"
         - On confirm: POST `/api/control/mode` with { target_state: target_state.toLowerCase() }
         - On success: show "Mode change sent" inline message (green text, auto-dismiss 3s)
         - On error: show error message (red text)

       - **Setpoint Input** (below mode selector):
         - Button "Set Power (kW)" opens NumericKeypad
         - NumericKeypad title: "Enter Power (kW)"
         - On confirm: open ConfirmDialog "Set power to {value} kW?"
         - On confirm: POST `/api/control/setpoint` with { power_kw: parseFloat(value) }
         - Show success/error inline

       - **Action Buttons** (bottom section):
         - Fault Reset button: always visible when in FAULT state. POST `/api/control/fault-reset` (no body). Confirm dialog: "Reset fault condition?"
         - Maintenance Enter button: visible only for admin users. POST `/api/control/maintenance` with { action: "enter" }. Confirm dialog: "Enter maintenance mode?" variant="danger"
         - Maintenance Exit button: visible only for admin users when in MAINTENANCE state. POST `/api/control/maintenance` with { action: "exit" }. Confirm: "Exit maintenance mode?"
         - All buttons >= 44x44px

       - **Feedback**: Use simple state for inline messages { text: string, type: "success" | "error" }. Auto-clear after 3 seconds with setTimeout.

    2. Write `__tests__/ControlScreen.test.tsx`:
       - Mock useTelemetry to return test system data
       - Mock useApi to return mock apiFetch
       - Mock useAuth to return test auth state
       - Test: renders current control state name
       - Test: renders 4 mode buttons
       - Test: clicking mode button opens confirm dialog
       - Test: confirming mode change calls apiFetch with correct target_state
       - Test: setpoint button opens numeric keypad
       - Test: fault reset button visible when control_state is FAULT (5)
       - Test: maintenance button visible only for admin level
       - Test: all buttons have minimum 44x44px dimensions (check class or style)
  </action>
  <verify>
    <automated>cd src/hmi_server/frontend && bun run test -- --run src/__tests__/ControlScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Control state displayed with color coding from live telemetry
    - Mode selector with 4 states, confirmation dialog before switching
    - Setpoint input via NumericKeypad with confirmation
    - Maintenance and fault reset buttons with proper visibility rules
    - All REST commands sent to correct endpoints
    - All tests pass, build succeeds
  </done>
</task>

</tasks>

<verification>
```bash
cd src/hmi_server/frontend && bun run build && bun run test -- --run
```
Build succeeds. All tests pass.
</verification>

<success_criteria>
- Alarm table shows severity-colored active alarms with ACK functionality
- Alarm history queryable via event_log REST endpoint
- Sidebar alarm badge shows unacked alarm count
- Control screen shows current state with mode selector and confirmation
- Setpoint entry via NumericKeypad touch interface
- Maintenance/fault-reset buttons properly gated by auth level and state
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-hmi-frontend/19-04-SUMMARY.md`
</output>
