---
phase: 09-foundation
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/config_manager/src/ems_config_manager/watcher.py
  - src/config_manager/src/ems_config_manager/manager.py
  - src/config_manager/src/ems_config_manager/__main__.py
  - src/config_manager/pyproject.toml
  - src/common/python/src/ems_common/ipc.py
  - src/common/c/include/ipc_defs.h
  - tests/test_config_manager.py
autonomous: true
requirements: [CONF-02, CONF-04, CONF-05, CONF-08]

must_haves:
  truths:
    - "Hot-reload detects file change via inotify IN_CLOSE_WRITE with 500ms debounce and applies validated config within 1 second"
    - "Failed hot-reload validation rejects change, keeps current config, publishes error event"
    - "Config backup is created before hot-reload changes are applied (keep last 5)"
    - "ZMQ REQ/REP serves get_config and get_value queries from in-memory cache"
    - "Config reload ZMQ event includes full validated config plus old-vs-new diff"
    - "system_config hot-reload is rejected (topology change requires restart)"
  artifacts:
    - path: "src/config_manager/src/ems_config_manager/watcher.py"
      provides: "InotifyWatcher with debounced hot-reload"
      min_lines: 50
    - path: "src/config_manager/src/ems_config_manager/__main__.py"
      provides: "config_manager service entry point running asyncio loop"
      min_lines: 30
    - path: "src/common/python/src/ems_common/ipc.py"
      provides: "SOCK_CONFIG socket path added"
      contains: "SOCK_CONFIG"
  key_links:
    - from: "src/config_manager/src/ems_config_manager/watcher.py"
      to: "config/*.yaml"
      via: "inotify IN_CLOSE_WRITE"
      pattern: "CLOSE_WRITE"
    - from: "src/config_manager/src/ems_config_manager/manager.py"
      to: "ipc:///run/ems/config.sock"
      via: "ZMQ REP socket"
      pattern: "config\\.sock"
    - from: "src/config_manager/src/ems_config_manager/manager.py"
      to: "ipc:///run/ems/logger.sock"
      via: "ZMQ PUSH for reload events"
      pattern: "SOCK_LOGGER|logger\\.sock"
---

<objective>
Add hot-reload (inotify + debounce + backup + diff), ZMQ REQ/REP config query API, and config_manager service entry point.

Purpose: Hot-reload enables runtime config changes for control/alarm/schedule without EMS restart. ZMQ API lets all modules query config over IPC. Together these complete CONF-02/04/05/08.
Output: Working config_manager asyncio service with inotify watcher, ZMQ server, and reload event publishing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-foundation/09-CONTEXT.md
@.planning/phases/09-foundation/09-RESEARCH.md
@.planning/phases/09-foundation/09-01-SUMMARY.md

@src/config_manager/src/ems_config_manager/manager.py
@src/common/python/src/ems_common/ipc.py
@src/common/c/include/ipc_defs.h

<interfaces>
From 09-01 (ConfigManager core):
```python
class ConfigManager:
    def __init__(self, config_dir: Path, schema_dir: Path, profile: str | None) -> None: ...
    def load_all(self) -> None: ...  # Fail-fast, populates _configs cache
    def get_config(self, name: str) -> dict: ...
    def get_value(self, name: str, path: str) -> Any: ...
    # _configs: dict[str, dict]  -- in-memory cache
```

From ems_common/ipc.py:
```python
SOCK_TELEMETRY: str = "ipc:///run/ems/telemetry.sock"
SOCK_LOGGER: str = "ipc:///run/ems/logger.sock"
TOPIC_CONFIG_RELOAD: str = "config_reload"
def encode_command_response(status, result=None, error_msg=None) -> bytes:
def decode_command_request(data) -> tuple[str, dict]:
def encode_event(timestamp_ms, source, severity, event_type, message, data=None) -> bytes:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IPC contract update and inotify hot-reload watcher</name>
  <files>
    src/common/python/src/ems_common/ipc.py,
    src/common/c/include/ipc_defs.h,
    src/config_manager/pyproject.toml,
    src/config_manager/src/ems_config_manager/watcher.py
  </files>
  <behavior>
    - Test: SOCK_CONFIG defined in both ipc.py and ipc_defs.h as "ipc:///run/ems/config.sock"
    - Test: Watcher detects IN_CLOSE_WRITE on a .yaml file and calls callback after 500ms debounce
    - Test: Watcher ignores non-yaml files and dot-prefixed temp files
    - Test: Watcher ignores system_config.yaml changes (topology change requires restart)
    - Test: Multiple rapid writes to same file result in only one callback (debounce)
  </behavior>
  <action>
  1. Add `SOCK_CONFIG: str = "ipc:///run/ems/config.sock"` to `src/common/python/src/ems_common/ipc.py` in the Socket paths section.

  2. Add `#define EMS_SOCK_CONFIG "ipc:///run/ems/config.sock"` to `src/common/c/include/ipc_defs.h` in the REQ/REP section.

  3. Update `src/config_manager/pyproject.toml` to add dependencies: `pyzmq>=26.0`, `inotify-simple>=1.3`, `msgpack>=1.0`.

  4. Create `src/config_manager/src/ems_config_manager/watcher.py`:
     - Class `ConfigWatcher` with:
       - `HOT_RELOAD_CONFIGS: set[str] = {"control_config", "alarms_config", "schedule_config"}`
       - `DEBOUNCE_S: float = 0.5`
       - Constructor takes `config_dir: Path` and `on_change: Callable[[str], Awaitable[None]]` callback.
       - `async watch_loop() -> None`: Uses inotify_simple to watch config_dir for `flags.CLOSE_WRITE`. Run `inotify.read()` in executor (it blocks). On event: extract stem from filename, skip if not in HOT_RELOAD_CONFIGS, skip dot-prefixed files. For matching files, cancel any pending debounce task and schedule new one with 500ms delay. After delay, call `on_change(name)`.
     - Per locked decisions: inotify debounce 500ms after last IN_CLOSE_WRITE, system_config rejected for hot-reload (not in HOT_RELOAD_CONFIGS set).

  5. Run `uv sync --all-packages` to install new dependencies.
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv sync --all-packages && uv run pytest tests/test_ipc_contracts.py -x -v</automated>
  </verify>
  <done>SOCK_CONFIG added to both C and Python IPC contracts, ConfigWatcher implements debounced inotify with correct file filtering</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ZMQ config API, backup, diff, and service entry point</name>
  <files>
    src/config_manager/src/ems_config_manager/manager.py,
    src/config_manager/src/ems_config_manager/__main__.py,
    tests/test_config_manager.py
  </files>
  <behavior>
    - Test: ZMQ REQ get_config("pcs_config") returns full config via REP
    - Test: ZMQ REQ get_value("pcs_config", "connection.protocol") returns correct value
    - Test: ZMQ REQ get_value with missing path returns error response with descriptive message
    - Test: ZMQ REQ with unknown action returns error response
    - Test: Hot-reload creates backup in config/backups/{name}.{timestamp}.yaml before applying
    - Test: Backup cleanup keeps only last 5 per config name
    - Test: Hot-reload publishes config_reload event with full new config and diff
    - Test: Failed hot-reload rejects change, keeps current config, publishes error event
    - Test: __main__.py starts asyncio loop with ConfigManager + watcher + ZMQ server
  </behavior>
  <action>
  1. Extend `src/config_manager/src/ems_config_manager/manager.py` with async methods:
     - `async serve_queries(ctx: zmq.asyncio.Context) -> None`: Bind ZMQ REP on SOCK_CONFIG. Loop: recv request, decode via `decode_command_request()`. Handle "get_config" (return full config dict) and "get_value" (navigate dotted path). Per locked decisions: missing path returns error response with descriptive message (not null), all queries served from in-memory cache.
     - `async handle_reload(name: str) -> None`: Called by watcher on_change callback.
       (a) Create backup: copy current file to `config/backups/{name}.{timestamp}.yaml`. Glob and delete oldest if >5 backups for this name (CONF-04).
       (b) Load and validate new file. If validation fails: log error, publish error event on SOCK_LOGGER with field path/expected/actual, return without changing cache (per locked decision: reject change, keep current running config).
       (c) Compute diff: compare old cached config vs new config. Use recursive dict comparison to find changed keys with old/new values. Produce a diff dict like `{"changed": {"alarms.thresholds.soc_low": {"old": 10, "new": 15}}, "added": {...}, "removed": {...}}`.
       (d) Swap cache atomically: `self._configs[name] = new_config`.
       (e) Publish config_reload event on SOCK_LOGGER (ZMQ PUSH) with: full new config in payload (per locked decision), diff as separate field for audit (CONF-08).
     - Add `_push_sock` (ZMQ PUSH to SOCK_LOGGER) as a member, initialized in startup.

  2. Create `src/config_manager/src/ems_config_manager/__main__.py`:
     - Parse CLI args: `--config-dir` (default: config/), `--schema-dir` (default: config/schemas/), `--profile` (optional, or EMS_PROFILE env var).
     - Create ConfigManager, call load_all() (fail-fast on error).
     - Create asyncio ZMQ context.
     - Create ConfigWatcher with on_change=manager.handle_reload.
     - Run asyncio.gather(manager.serve_queries(ctx), watcher.watch_loop()).
     - Handle SIGTERM/SIGINT for graceful shutdown.

  3. Add tests to `tests/test_config_manager.py`:
     - ZMQ query tests: Use zmq.asyncio in tests to send REQ and verify REP responses.
     - Hot-reload tests: Write modified YAML to tmp_path, trigger handle_reload, verify backup created, cache updated, event published.
     - Failed reload test: Write invalid YAML, trigger handle_reload, verify cache unchanged, error event published.
     - Backup cleanup test: Trigger 7 reloads, verify only 5 backups remain.
     - Diff test: Modify one field, verify diff in reload event contains correct old/new values.
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv run pytest tests/test_config_manager.py -x -v</automated>
  </verify>
  <done>ZMQ REQ/REP serves config queries from cache, hot-reload creates backup before applying, failed reload keeps current config and publishes error, reload event includes full config + diff, service runs as asyncio loop</done>
</task>

</tasks>

<verification>
1. Config manager tests pass: `uv run pytest tests/test_config_manager.py -x -v`
2. IPC contract tests still pass: `uv run pytest tests/test_ipc_contracts.py -x -v`
3. Full suite: `uv run pytest tests/ -x`
</verification>

<success_criteria>
- SOCK_CONFIG defined in both Python and C IPC contracts
- Hot-reload detects changes via inotify with 500ms debounce, only for control/alarms/schedule configs
- Backup created before each hot-reload (last 5 kept)
- Failed validation rejects change, keeps current config, publishes error event with details
- ZMQ REQ/REP serves get_config and get_value from in-memory cache
- Config reload event includes full validated config plus old-vs-new diff
- config_manager runs as asyncio service via __main__.py
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation/09-03-SUMMARY.md`
</output>
