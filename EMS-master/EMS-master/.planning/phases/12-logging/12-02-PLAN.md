---
phase: 12-logging
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/logger/python/src/ems_logger/telemetry_writer.py
  - src/logger/python/tests/test_telemetry_writer.py
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-08
  - LOG-09

must_haves:
  truths:
    - "1Hz ZMQ SUB messages are written to Parquet files with Snappy compression"
    - "Parquet files rotate on hour boundary using message timestamp, not wall clock"
    - "Files are organized as data/{year}/{month}/{day}/telemetry_{cluster}_{hour}.parquet"
    - "System telemetry goes to telemetry_system_{hour}.parquet"
    - "Incomplete writes use .tmp files renamed atomically on rotation"
    - "Topology metadata is stored in Parquet file metadata"
  artifacts:
    - path: "src/logger/python/src/ems_logger/telemetry_writer.py"
      provides: "ParquetRotatingWriter and TelemetryWriter classes"
      exports: ["ParquetRotatingWriter", "TelemetryWriter"]
      min_lines: 150
    - path: "src/logger/python/tests/test_telemetry_writer.py"
      provides: "Tests for Parquet write, rotation, directory structure, compression"
      min_lines: 100
  key_links:
    - from: "src/logger/python/src/ems_logger/telemetry_writer.py"
      to: "src/logger/python/src/ems_logger/parquet_schema.py"
      via: "imports build_cluster_schema, build_system_schema"
      pattern: "from ems_logger\\.parquet_schema import"
    - from: "src/logger/python/src/ems_logger/telemetry_writer.py"
      to: "ipc:///run/ems/telemetry.sock"
      via: "ZMQ SUB socket subscription"
      pattern: "SOCK_TELEMETRY"
---

<objective>
Parquet telemetry writer: subscribes to ZMQ PUB telemetry, buffers 1-second snapshots, writes per-cluster and system Parquet files with Snappy compression, rotates hourly.

Purpose: This is the core data persistence engine -- all RTDB telemetry flows through here to disk. Addresses the primary data retention requirement.
Output: TelemetryWriter class consuming ZMQ SUB and writing Parquet via PyArrow streaming writer.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-logging/12-CONTEXT.md
@.planning/phases/12-logging/12-RESEARCH.md
@.planning/phases/12-logging/12-01-SUMMARY.md

@src/logger/python/src/ems_logger/parquet_schema.py
@src/logger/python/src/ems_logger/config.py
@src/logger/python/tests/conftest.py
@src/data_manager/python/src/ems_data_manager/publisher.py
@src/common/python/src/ems_common/ipc.py

<interfaces>
From parquet_schema.py (created in Plan 01):
```python
def build_cluster_schema(racks_per_cluster: int, modules_per_rack: int, cells_per_module: int, temps_per_module: int) -> pa.Schema
def build_system_schema() -> pa.Schema
def build_topology_metadata(cluster_count: int, racks_per_cluster: int, modules_per_rack: int, cells_per_module: int) -> dict[str, str]
```

From config.py (created in Plan 01):
```python
class LoggerConfig:
    storage: StorageConfig  # data_dir, parquet_retention_days, etc.
    parquet: ParquetConfig  # compression, rotation_period_s, row_group_size
```

Telemetry envelope from ipc.py:
```python
# Multipart: [topic_bytes, encoded_envelope]
# Envelope: {ts: int, seq: int, src: str, topic: str, payload: dict}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParquetRotatingWriter with atomic .tmp rename</name>
  <files>
    src/logger/python/src/ems_logger/telemetry_writer.py,
    src/logger/python/tests/test_telemetry_writer.py
  </files>
  <behavior>
    - ParquetRotatingWriter(schema, data_dir, file_prefix, metadata) manages a single Parquet file
    - write_row(row_dict) writes one row; rotates file when hour changes (based on ts in row_dict)
    - Current file always has .tmp extension; renamed to .parquet on close/rotate
    - close() finalizes current .tmp file with atomic rename
    - Directory structure: data_dir/{year}/{month:02d}/{day:02d}/{prefix}_{hour:02d}.parquet
    - Parquet files use Snappy compression (LOG-08)
    - Topology metadata dict stored in Parquet file metadata (LOG-09)
    - If .parquet already exists for current hour (restart scenario), writer creates new file (previous hour's data already committed)
  </behavior>
  <action>
    Create ParquetRotatingWriter class in telemetry_writer.py:
    - __init__ takes pa.Schema, data_dir Path, file_prefix str, metadata dict[str, str]
    - write_row(row_dict: dict) determines hour from row_dict["ts"] (ms epoch), opens/rotates as needed, writes pa.RecordBatch
    - _open_new(ts_ms) creates day directory, opens ParquetWriter to .tmp file with Snappy compression and metadata
    - _close_current() calls writer.close(), then os.rename(.tmp -> .parquet)
    - close() for clean shutdown
    - Handle edge case: if _open_new is called but .tmp already exists (stale from crash), delete it first

    Write tests in test_telemetry_writer.py:
    - test_parquet_write_from_telemetry: write 5 rows, close, verify Parquet readable with correct data
    - test_parquet_hourly_rotation: write rows spanning 2 hours, verify 2 files created
    - test_parquet_directory_structure: verify data/{year}/{month}/{day}/ path
    - test_snappy_compression: verify Parquet metadata shows snappy codec
    - test_atomic_tmp_rename: verify no .tmp files after close, .parquet exists
    - test_topology_metadata_in_file: verify metadata readable from Parquet file
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv run pytest src/logger/python/tests/test_telemetry_writer.py -x -v</automated>
  </verify>
  <done>ParquetRotatingWriter writes Snappy-compressed Parquet files with hourly rotation, atomic .tmp rename, correct directory structure, and topology metadata</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TelemetryWriter ZMQ SUB consumer with 1-second buffering</name>
  <files>
    src/logger/python/src/ems_logger/telemetry_writer.py,
    src/logger/python/tests/test_telemetry_writer.py
  </files>
  <behavior>
    - TelemetryWriter subscribes to ZMQ PUB telemetry and collects all topics within a 1-second window
    - After 1 second, writes one row to each active ParquetRotatingWriter (cluster files + system file)
    - BMS rack topics go to per-cluster writers; PCS/GPIO/Meter/BTMS/System topics go to system writer
    - Topic routing: "bms.rack.{c}.{r}" -> cluster_{c} writer; "pcs"/"gpio"/"meter"/"btms"/"system" -> system writer
    - Missing topics in a 1-second window produce null/zero values in that row
  </behavior>
  <action>
    Add TelemetryWriter class to telemetry_writer.py:
    - __init__(config: LoggerConfig, zmq_ctx, topology: dict, endpoint: str | None = None)
    - Creates one ParquetRotatingWriter per cluster (prefix "telemetry_{cluster}") and one for system (prefix "telemetry_system")
    - _collect_window() async: receive all ZMQ messages for up to 1 second, group by topic
    - _build_cluster_row(cluster_idx, rack_payloads, ts_ms) -> dict mapping topic payloads to schema columns
    - _build_system_row(section_payloads, ts_ms) -> dict mapping PCS/GPIO/Meter/BTMS/System payloads to schema columns
    - run() async: main loop calling _collect_window then writing rows
    - close(): close all writers
    - Subscribe to all telemetry topics: TOPIC_BMS_RACK, TOPIC_PCS, TOPIC_GPIO, TOPIC_METER, TOPIC_BTMS, TOPIC_SYSTEM

    Add tests:
    - test_telemetry_topic_routing: send mixed topics via ZMQ, verify cluster vs system file assignment
    - test_telemetry_1s_buffering: send multiple topic messages, verify single row per second per file
    - test_telemetry_missing_topic_fills_defaults: omit a topic, verify row still written with defaults
  </action>
  <verify>
    <automated>cd /home/overlord/Dev/Revx_Energy/EMS && uv run pytest src/logger/python/tests/test_telemetry_writer.py -x -v</automated>
  </verify>
  <done>TelemetryWriter subscribes to ZMQ PUB, buffers 1-second windows, routes topics to correct per-cluster or system Parquet writers, handles missing topics gracefully</done>
</task>

</tasks>

<verification>
- Parquet files are readable by pyarrow.parquet.read_table()
- Files use Snappy compression (check metadata)
- Hourly rotation creates new files with correct naming
- Directory structure matches data/{year}/{month}/{day}/
- No .tmp files remain after clean close
- ZMQ topic routing correctly separates BMS racks from system data
</verification>

<success_criteria>
- 1Hz telemetry messages produce correctly structured Parquet files (LOG-01)
- Files rotate on hour boundary based on message timestamp (LOG-02)
- Snappy compression confirmed in file metadata (LOG-08)
- Files written to data/{year}/{month}/{day}/telemetry_{cluster}_{hour}.parquet (LOG-09)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-logging/12-02-SUMMARY.md`
</output>
